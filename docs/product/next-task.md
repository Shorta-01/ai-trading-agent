# Task 153 — Add real IBKR paper-only read-only account snapshot preflight for cash and positions without persistence or valuation

Task 152-R4 is completed as a repair-only merged-red fix after Task 152-R3 (`api` job `pytest`), updating stale fake-client manual status-check pytest expectations to the stricter Task 152 gate model while preserving disabled-by-default runtime behavior and no-secret safety checks.

Continue with Task 153 only, while preserving current safety boundaries, disabled-by-default runtime behavior, and readiness semantics.
