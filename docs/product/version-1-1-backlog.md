## V1.1 status

**V1.1 is in flight.** V1 (Slices 1–22) is feature-complete and the
morning chain runs end-to-end against the locked paper-account
configuration. V1.1 refactors the prediction stack and widens four
non-prediction areas, all locked via §22 in
`version-1-product-experience-locks.md`.

The V1 predictor stack stays running through the entire V1.1
refactor — each refactor slice ships behind a per-slice flag and the
morning chain never goes dark.

## V1.1 slice queue (Slices 23–34, locked via §22)

- Slice 23 — Task 178: **V1.1 doctrine amendment**. Lock the V1.1 scope into `version-1-product-experience-locks.md §22`; create this backlog + `version-1-1-scope-register.md`; add the placeholder `CLAUDE_AI_BUDGET_MONTHLY_EUR` setting and the commented-out numpy/pandas/statsmodels declarations in `pyproject.toml`. Docs-only; no behavior change.
- Slice 24 — Task 179 ✅ **completed**: Predictor refactor base. Heavy deps activated (`numpy>=2.0.0` + `pandas>=2.2.0` + `statsmodels>=0.14.0` in `packages/portfolio`'s runtime dependencies). New `_predictor_math` module centralises numpy-backed primitives (`bar_closes_array`, `log_returns`, `sample_mean`, `sample_stdev`, `population_stdev`, `normal_cdf`, `Z_10/50/90`, `decimal_from_float`, `clipped_probability`). All four deterministic predictors (`baseline_forecast`, `momentum_predictor`, `mean_reversion_predictor`, `qvm_factor_predictor`) routed through the shared module; behaviour identical (existing pytest suite passes unchanged). `PredictorResearchProtocol` extends `PredictorProtocol` with `backtest_window_score(inputs, *, window_days) -> BacktestWindowScore`. Storage migration `0041_predictor_backtest_runs` + `PredictorBacktestRunRecord` (status set `{running, succeeded, failed, skipped}`, hit_rate ∈ [0, 1] when set, non-negative Brier, safety booleans hard-False) + `SqlAlchemyPredictorBacktestRunRepository` (save / update / list with filter). `claude_ai_budget_monthly_eur` + `universe_set` + `predictor_backtest_enabled` settings declared from Slice 23 stay; no new env vars in this slice.
- Slice 25 — Task 180 ✅ **completed**: Backtesting framework. New `predictor_backtester` module in `packages/portfolio` with pure-numpy walk-forward harness: `walk_forward_backtest(predictor, bars, *, window_days, horizon_trading_days, step_days)` slides a window forward by `step_days` bars, scores each fold against `bars[end:end+horizon]`, never lets a fold's predictor see beyond `bars[end]`. Per-fold scoring computes Brier (predicted prob_gain vs realised gain indicator), hit-rate (predicted direction collapsed to up/flat/down vs realised), and Sharpe (realised-return mean ÷ std). `aggregate_window_score` returns `None` metrics when folds < 2. `run_predictor_backtest(...)` surfaces the `BacktestPersistenceOutputs` (status `succeeded` / `skipped`, `blocking_reason="insufficient_folds"`). `backtest_window_score_for_predictor(...)` is the single-line helper every predictor wires through. All five V1 predictors (`GbmPredictor`, `MomentumPredictor`, `MeanReversionPredictor`, `QvmFactorPredictor`, `AiTsPredictor`) now implement `backtest_window_score(inputs, *, window_days) -> BacktestWindowScore`. New `predictor_backtest_orchestrator` module in `apps/api` wires bars from `SqlAlchemyMarketDataBarRepository` to the harness; `LOCKED_MODEL_CODES = {baseline_gbm, momentum_v1, mean_reversion_v1}` defines the Slice 25 supported set; `qvm_factor_v1` + `ai_ts_v1` return `skipped` with stable reasons (`qvm_backtest_deferred_to_slice_28`, `ai_ts_backtest_deferred_to_slice_30`); unknown codes return `skipped` with `unknown_model_code`; empty bars return `no_bars_persisted`. Two new routes: `POST /predictor/backtest/run` (gated on `predictor_backtest_enabled` + writable storage; body `{model_code, asset_symbol, ibkr_conid, window_days?, horizon_trading_days?, step_days?}`; persists running + terminal `PredictorBacktestRunRecord`) and `GET /predictor/backtest/latest?model_code=&asset_symbol=&limit=25` (leaderboard surface). 30 new tests (16 backtester harness + 8 orchestrator + 6 endpoint); ruff + mypy clean.
- Slice 26 — Task 181 ✅ **completed**: Feedback loop + auto-weighting. Storage migration `0042_prediction_diary_per_predictor` adds child table `prediction_diary_predictor_contributions` keyed on `(diary_entry_id, model_code)` with the per-predictor predicted return / prob_gain / direction + realised return / direction / outcome label + single-fold Brier + signed return-spread; safety booleans hard-False; `PredictionDiaryPredictorContributionRecord` invariants enforce non-empty IDs, prob_gain ∈ [0, 1], non-negative Brier, hard-False safety booleans. `SqlAlchemyPredictionDiaryPredictorContributionRepository` (save + list with optional model_code + diary_entry_id filter). New `predictor_feedback` module in `packages/portfolio`: `compute_per_predictor_outcomes(*, ensemble, realised_return_pct, now=None)` emits one `PerPredictorOutcome` per surviving predictor with `outcome_label ∈ {correct, partial, wrong, no_data}` and the single-fold Brier; `compute_inverse_brier_weights(history, *, clip=(0.05, 0.40), fallback_codes=())` computes `1/brier`-weighted, clipped, normalised dict; iterative water-filling handles the cross-impact of the two clip bounds; falls back to equal-weight when the bounds are infeasible (e.g. N < 3 for the locked [0.05, 0.40] band). Ensemble combiner gains `weight_strategy: "equal_weight" | "auto"` (default `equal_weight`); `auto` consumes `brier_history` and threads through `compute_inverse_brier_weights` with the surviving predictors as `fallback_codes` so a fresh predictor never gets silenced. New setting `ensemble_weight_strategy` (default `"equal_weight"`). New route `GET /predictor/leaderboard?asset_symbol=` reads the latest succeeded backtest row per model_code, computes the inverse-Brier auto-weights, returns both as JSON; read-only; safety booleans hard-False. **27 new tests** (12 feedback math + 2 storage record/repo + 9 endpoint + ensemble strategy switch). Manual approval gate stays; AI is still one vote — the auto-weight strategy may down-weight it but never silence it.
- Slice 27 — Task 182 ✅ **completed**: GBM + Momentum rebuild. `compute_baseline_forecast(...)` gains four opt-in kwargs: `drift_window_days` (caps drift estimation to last N bars; volatility still uses full series), `regime_shift_enabled` + `regime_shift_threshold_pct` (50/50 blend of long-window drift with the rolling 60-day drift when divergence exceeds threshold), `garch_enabled` (declared but raises `NotImplementedError` — the GARCH(1,1) volatility path lands in a follow-up slice once the Slice 26 leaderboard measures baseline). `GbmPredictor.__init__` threads the same kwargs. `MomentumPredictor` gains two opt-in kwargs: `horizon_scaled_thresholds` (direction thresholds scale as `±X% × √(horizon/21)` so a 5-day horizon's slight bucket is narrower than a 60-day one) and `skip_week_short_horizon` (11-week skip-1-week momentum replaces skip-1-month for horizons < 21 days). Threshold constants `DIRECTION_THRESHOLD_STRONG_PCT=10.0` + `DIRECTION_THRESHOLD_SLIGHT_PCT=2.0` + `DIRECTION_THRESHOLD_BASELINE_HORIZON=21` exposed at module scope; new helper `_direction_label_for_horizon` mirrors the locked bucket set under the √(horizon/21) scaling. New `apps/api/config.py` settings: `gbm_drift_window_days` (None default), `gbm_regime_shift_enabled` (False default), `gbm_regime_shift_threshold_pct` (Decimal `5.0` default), `gbm_garch_enabled` (False default — set True only when a follow-up slice ships the GARCH path), `momentum_horizon_scaled_thresholds` (False default), `momentum_skip_week_short_horizon` (False default). `predictor_backtest_orchestrator.run_backtest_for_symbol(...)` gains a `settings` parameter and threads the rebuild knobs into each predictor at construction time so the Slice 26 leaderboard can measure the rebuilt predictors against the V1 baselines. **Defaults preserve V1 behaviour exactly** — every existing predictor test still passes unchanged; the rebuilt path activates only when the operator opts in via env var. 19 new tests (9 GBM rebuild + 10 Momentum rebuild). All 1172 tests green; ruff + mypy clean.
- Slice 28 — Task 183 ✅ **completed**: Mean-Rev + QVM rebuild. `MeanReversionPredictor` gains opt-in `hurst_asymmetric_target` kwarg — when True, a new `_hurst_asymmetric_target_price(...)` helper blends the SMA-pull target with a trend extrapolation based on the Hurst exponent: ``H ≤ 0.45`` → full SMA pull (V1); ``H ≥ 0.55`` → full trend extrapolation (no reversion); in-between → linear blend on H. Trend extrapolation is a simple 20-day return continuation. `QvmFactorPredictor` gains three opt-in kwargs: `minimum_universe_size` (raises the per-instance floor; the `apps/api` setting `qvm_min_universe_size` defaults to 30 — the V1.1 §22.5 industry-practice floor — while the dataclass default stays at 5 for backward compat), `sector_neutral_zscore` (de-means within sector before z-scoring; new `_sector_neutral_factor_score_for_symbol(...)` helper; falls back to global mean when sector data is missing), `soft_clip_composite` (replaces the hard ±2 linear map onto [-1, +1] with a soft tanh map via new `_soft_clip_tanh(...)` helper). Four new settings in `apps/api/config.py`: `mean_reversion_hurst_asymmetric_target`, `qvm_min_universe_size`, `qvm_sector_neutral_zscore`, `qvm_soft_clip_composite`. `predictor_backtest_orchestrator._build_predictor_or_skip(...)` threads the new Mean-Rev knob into the predictor construction; QVM stays in the deferred set with the stable reason `qvm_backtest_deferred_to_slice_28` because the walk-forward harness still needs a universe snapshot per fold (a future widening). **Defaults preserve V1 behaviour exactly** — every existing predictor test still passes unchanged. 15 new tests (7 Mean-Rev rebuild + 8 QVM rebuild). All 1187 tests green; ruff + mypy clean.
- Slice 29 — Task 184 ✅ **completed**: Real AI explanation provider. Adds anthropic SDK as a runtime dep of apps/api. New `claude_ai_budget.py` module owns the monthly budget surface: `compute_cost_eur(...)`, `monthly_budget_status(...)`, `assert_budget_available(...)`, `persist_call_cost(...)`, `budget_month_of(...)`; locked default rates (€0.80/€0.08/€4.00 per million input/cached/output units for Claude Haiku 4.5); raises `ClaudeAiBudgetExceededError` when the cap hits. Storage migration `0043_claude_ai_budget_usage` adds the audit table; `ClaudeAiBudgetUsageRecord` with invariants (call_kind ∈ {explanation, ts_forecast}, budget_month YYYY-MM format, non-negative units + cost, hard-False safety booleans); `SqlAlchemyClaudeAiBudgetUsageRepository` (`save_usage`, `monthly_total_eur(month)`, `list_recent_usage`). New `anthropic_explanation_provider.py` module: `AnthropicExplanationProvider` implements `ExplanationProviderProtocol`; locked Dutch `SYSTEM_PROMPT_NL` + the V1 legal disclaimer ship with ephemeral `cache_control` markers (mandatory prompt caching); each call asserts the budget, posts via the SDK's messages.create, decodes `cache_read_input_tokens` as cached units and `input_tokens + cache_creation_input_tokens` as paid input, persists one audit row, returns the structured `ExplanationProviderResult`. Lazy SDK import keeps the module loadable in stub-only deployments. Factory `build_explanation_provider(settings, *, budget_repo)` returns the real client when `ai_explanation_enabled` + `ai_explanation_real_client_enabled` + `provider_code=anthropic_claude` + `claude_ai_api_key` set + `budget_repo` provided, otherwise falls back to stub or surfaces stable `ExplanationProviderUnavailable` reason codes (`claude_ai_api_key_missing`, `claude_ai_budget_repo_missing`). New settings: `claude_ai_explanation_model` (default `claude-haiku-4-5-20251001`), `claude_ai_api_key` (env-only, no committed default), `claude_ai_explanation_max_output_chars` (default 2000). 23 new tests (12 budget math + 9 Anthropic provider + 2 storage record/repo). All 1210 tests green; ruff + mypy clean.
- Slice 30 — Task 185 ✅ **completed**: Real AI TS predictor. New `anthropic_ts_provider.py` module: `AnthropicTsModelProvider` issues a tool-use call to Claude that forces the response into the locked `TsModelProviderResult` shape via the `emit_ts_forecast` tool with a JSON schema mirroring the Slice 18 dataclass; `_extract_tool_call`, `_validate_payload` (quantile ordering, prob_gain ∈ [0, 1], confidence ∈ [0, 1]), `_build_messages_payload` with ephemeral cached system prompt. Soft errors (provider returned no tool_use, payload validation fails, SDK raises, budget exceeded) map to `TsModelProviderUnavailable(reason=…)` so the existing Slice 18 `AiTsPredictor` blocks cleanly. Budget enforcement reuses Slice 29's `claude_ai_budget` module; calls land in the same `claude_ai_budget_usage` audit table under `call_kind="ts_forecast"` — shared monthly cap across explanation + TS calls. **§22.2 daily-only invocation**: `build_ts_model_provider(settings, *, budget_repo, invoked_from_scheduler=False)` returns the real client only when `invoked_from_scheduler=True` (the scheduler-driven morning chain sets this; on-demand routes leave it False and get `daily_only_invocation_required`). The `timesfm` provider-code returns the stable `timesfm_not_implemented` reason; the `anthropic_claude` code routes through the new path. Two new settings: `ai_ts_predictor_daily_only` (default True, the §22.2 lock) + `ai_ts_predictor_max_tokens` (default 1024). 15 new tests (3 happy path + 1 budget cap + 4 malformed response handling + 7 factory gates). All 1225 tests green; ruff + mypy clean.
- Slice 31 — Task 186: **Universe scan expansion + operator-selectable set**. Lock-universe registry adds `EU600` + `ALL_5K`. New `UNIVERSE_SET` env var (default `SP500`). Per-set EODHD-call caching + storage paging.
- Slice 32 — Task 187: **Conditional orders + GTC/OPG/IOC**. Extend `LOCKED_ORDER_TYPES` with `CONDITIONAL`. Extend TIF to `{DAY, GTC, OPG, IOC}`. New draft fields for price / time / margin / volume / execution conditions. Per-type dry-run safety codes. `ibapi.Order.conditions` + `Order.tif` wiring.
- Slice 33 — Task 188: **UX upgrade**. Predictor-leaderboard panel; backtest-history panel; Decision Package diff between morning runs; Dutch microcopy review across the existing Portefeuille surface.
- Slice 34 — Task 189: **V1.1 release readiness**. Updated readiness scorecard with V1.1 blockers; V1.1 acceptance test (end-to-end against stub providers); runbook update. Locks the V1.1 expansion queue closed.

## Out of V1.1 scope (post-V1.1 widening)

- Multi-account portfolios.
- Mobile app.
- Real-time intraday predictor evaluation.
- Briefing item source distinction (portfolio / watchlist / universe_scan_candidate).
- Real-money path (V1.1 stays paper-first; a separate scope discussion locks any live-money slice).
- New predictor families beyond the V1 five (volatility-as-prediction, options-implied probs, sentiment-from-research-as-predictor, PEAD post-earnings-drift) — deferred to V1.2 once V1.1's feedback loop measures the existing five.
