- Task 183 completed: in scope: V1.1 Mean-Rev + QVM rebuild (Slice 28 of V1.1). `MeanReversionPredictor.__init__` gains `hurst_asymmetric_target: bool = False`. New helpers in `mean_reversion_predictor.py`: `_hurst_asymmetric_target_price(*, current_price, sma, pull, hurst, prices)`. Locked thresholds: `H ≤ 0.45` (full SMA), `H ≥ 0.55` (full trend), linear blend in between. `QvmFactorPredictor.__init__` gains `minimum_universe_size: int = QVM_MIN_UNIVERSE_SIZE`, `sector_neutral_zscore: bool = False`, `soft_clip_composite: bool = False`. New helpers in `qvm_factor_predictor.py`: `_sector_neutral_factor_score_for_symbol(...)`, `_soft_clip_tanh(value)`. New constant `QVM_MIN_UNIVERSE_SIZE_V1_1 = 30`. The `predict(...)` path reads `self._minimum_universe_size` so each instance honours its own floor. Four new settings in `apps/api/config.py`: `mean_reversion_hurst_asymmetric_target`, `qvm_min_universe_size` (default `30`), `qvm_sector_neutral_zscore`, `qvm_soft_clip_composite`. `predictor_backtest_orchestrator._build_predictor_or_skip(...)` threads the Mean-Rev knob. QVM stays in the deferred-skip set with `qvm_backtest_deferred_to_slice_28`. Out of scope (deferred to later slices): QVM walk-forward harness with per-fold universe snapshots (deferred — the EODHD universe-scan history needs to mature; a future post-V1.1 slice will add the snapshot-per-fold model); wiring the rebuild knobs into the morning chain's forecast path (Slice 31+); real AI explanation provider (Slice 29); real AI TS predictor (Slice 30); UX panels for the rebuilt predictors (Slice 33's UX upgrade).
- Task 182 completed: in scope: V1.1 GBM + Momentum rebuild (Slice 27 of V1.1). `compute_baseline_forecast(...)` gains opt-in kwargs `drift_window_days`, `regime_shift_enabled`, `regime_shift_threshold_pct` (default `5.0`), `garch_enabled` (raises `NotImplementedError`); new helper `_annualised_drift_with_regime_blend(...)`; module-scope constants `DEFAULT_DRIFT_WINDOW_DAYS_V1_1`, `DEFAULT_REGIME_SHIFT_SHORT_WINDOW`, `DEFAULT_REGIME_SHIFT_THRESHOLD_PCT`. `GbmPredictor.__init__` threads `drift_window_days`/`regime_shift_enabled`/`regime_shift_threshold_pct`/`garch_enabled`. `momentum_predictor.py` gains module-scope constants `DIRECTION_THRESHOLD_STRONG_PCT`/`DIRECTION_THRESHOLD_SLIGHT_PCT`/`DIRECTION_THRESHOLD_BASELINE_HORIZON`/`LOOKBACK_WEEK`/`LOOKBACK_11_WEEKS`/`SHORT_HORIZON_CUTOFF_DAYS`; new helpers `_direction_label_for_horizon(...)` and `_compute_skip_the_week_momentum(...)`. `MomentumPredictor.__init__` gains `horizon_scaled_thresholds: bool = False` and `skip_week_short_horizon: bool = False`. Six new settings in `apps/api/config.py`: `gbm_drift_window_days`, `gbm_regime_shift_enabled`, `gbm_regime_shift_threshold_pct`, `gbm_garch_enabled`, `momentum_horizon_scaled_thresholds`, `momentum_skip_week_short_horizon`. `predictor_backtest_orchestrator.run_backtest_for_symbol(...)` gains `settings: object | None = None` parameter threaded through `_build_predictor_or_skip(...)` so the GBM + Momentum predictors are built with the operator's rebuild knobs; the existing V1 path stays invariant when `settings=None`. Out of scope (deferred): real GARCH(1,1) volatility wiring (declared via `garch_enabled` but raises `NotImplementedError` until a follow-up slice), wiring the rebuild knobs into the morning chain's actual forecast path (deferred to Slice 31+ once the morning chain calls the combiner — Slice 25's backtester is the only live caller in V1.1), the Mean-Rev + QVM rebuilds (Slice 28), QVM minimum-universe floor raise (Slice 28), sector-neutral QVM z-scoring (Slice 28), soft-tanh QVM composite mapping (Slice 28), volatility-adjusted momentum composite score (deferred — TSM already does this in a sense).
- Task 181 completed: in scope: V1.1 feedback loop + auto-weighted ensemble (Slice 26 of V1.1, the biggest single lever from the Slice-22 audit). Storage migration `0042_prediction_diary_per_predictor` adds child table `prediction_diary_predictor_contributions` keyed on `(diary_entry_id, model_code)` with `(predicted_return_pct, predicted_prob_gain, predicted_direction, realised_return_pct, realised_direction, outcome_label, brier_score, return_spread_pct, explanation_nl, created_at)` columns + indexes on `diary_entry_id` and `(model_code, created_at)`. `PredictionDiaryPredictorContributionRecord` invariants: non-empty IDs, `predicted_prob_gain ∈ [0, 1]`, non-negative Brier, hard-False safety booleans. `SqlAlchemyPredictionDiaryPredictorContributionRepository` (save + list with `model_code` + `diary_entry_id` filter). New `predictor_feedback` module: `compute_per_predictor_outcomes(*, ensemble, realised_return_pct, now)` returns one `PerPredictorOutcome` per surviving predictor; `compute_inverse_brier_weights(history, *, clip, fallback_codes)` with iterative two-pass water-filling. Locked outcome labels `{correct, partial, wrong, no_data}`. Locked clip band `(0.05, 0.40)`. Ensemble combiner gains `weight_strategy: "equal_weight" | "auto"`; rejects unknown strategies; `auto` lazy-imports `compute_inverse_brier_weights` to avoid a circular dependency. New setting `ensemble_weight_strategy` (default `equal_weight`). New route `GET /predictor/leaderboard?asset_symbol=` aggregates the latest succeeded backtest row per `model_code`, surfaces the inverse-Brier auto-weights + the current `weight_strategy`; safety booleans hard-False. Out of scope (deferred to later slices): wiring the `weight_strategy` parameter into `forecast_sync` (deferred — the combiner only has test callers today; Slices 27 + 28 predictor rebuilds will exercise the live path); persisting per-predictor outcomes via the existing `prediction_diary_sync` orchestrator (deferred to the same wiring slice); UI panel surfacing the leaderboard (Slice 33's UX upgrade); rolling-window Brier aggregation across multiple backtest runs per predictor (V1.1 keeps the latest-succeeded-row strategy; a rolling average lands when needed); per-asset auto-weights (V1.1 uses one global auto-weight set across the universe; per-asset weighting is a post-V1.1 widening).
- Task 180 completed: in scope: V1.1 backtesting framework (Slice 25 of V1.1). New `predictor_backtester` module (numpy walk-forward harness); `walk_forward_backtest`, `aggregate_window_score`, `walk_forward_score`, `run_predictor_backtest`, `backtest_window_score_for_predictor`, `new_backtest_run_id`. `FoldOutcome` value object; `BacktestPersistenceInputs` + `BacktestPersistenceOutputs`. Five V1 predictors gain `backtest_window_score(inputs, *, window_days)`. New `predictor_backtest_orchestrator` module in apps/api with `LOCKED_MODEL_CODES = {baseline_gbm, momentum_v1, mean_reversion_v1}`, `SKIPPED_QVM_REASON`, `SKIPPED_AI_TS_REASON`, `run_backtest_for_symbol`, `serialize_backtest_run_record`. Routes: `POST /predictor/backtest/run` (gated on `predictor_backtest_enabled` + writable storage) + `GET /predictor/backtest/latest?model_code=&asset_symbol=&limit=25`. Per-fold predictor exceptions caught at boundary; the orchestrator never raises. Out of scope (deferred to later slices): real QVM walk-forward with per-fold universe snapshots (Slice 28); real AI provider for AI TS backtest (Slice 30); per-predictor outcome persistence in the Prediction Diary + auto-weighted ensemble strategy (Slice 26); GBM / Momentum / Mean-Rev rebuilds (Slices 27 + 28); universe-set selection (Slice 31); conditional orders (Slice 32); UX leaderboard panel (Slice 33).
- Task 179 completed: in scope: V1.1 predictor refactor base (Slice 24 of V1.1). New `_predictor_math` module (numpy-backed `bar_closes_array`, `log_returns`, `sample_mean`, `sample_stdev`, `population_stdev`, `normal_cdf`, `Z_10/50/90`, `decimal_from_float`, `clipped_probability`). All four deterministic predictors (`baseline_forecast`, `momentum_predictor`, `mean_reversion_predictor`, `qvm_factor_predictor`) routed through the shared module — behaviour identical (every existing test passes unchanged). `predictor_protocol` extended with `BacktestWindowScore(model_code, model_version, window_days, bars_used, brier_score, hit_rate, sharpe_ratio, explanation_nl)` frozen dataclass and `PredictorResearchProtocol(model_code, model_version, predict, backtest_window_score)` Protocol. Storage migration `0041_predictor_backtest_runs` adds audit table; `PredictorBacktestRunRecord` (status set `{running, succeeded, failed, skipped}`, positive `window_days`, non-negative `bars_used`, `hit_rate ∈ [0, 1]` when set, non-negative `brier_score`, safety booleans hard-False) + `SqlAlchemyPredictorBacktestRunRepository` (save / update / list with optional model_code + asset_symbol filter). Pyproject deps activated; mypy ignore-list extended for `scipy.*`. Out of scope (confirmed not in this slice, deferred): real walk-forward harness implementation (Slice 25); `backtest_window_score(...)` implementations on each predictor (Slice 25); routes for backtest run + leaderboard (Slice 25); per-predictor outcome persistence in the Diary (Slice 26); auto-weighted ensemble strategy (Slice 26); behaviour changes inside any predictor (Slice 27 + 28); real AI providers (Slices 29 + 30); universe expansion (Slice 31); conditional orders (Slice 32); UX panels for the leaderboard (Slice 33). The V1 morning chain continues to run on the V1 predictors unchanged.
- V1.1 scope locked via owner sign-off after V1 ship. Locked decisions:
  - Heavy dependency lock relaxed: `numpy` + `pandas` + `statsmodels` allowed inside `packages/portfolio`; `packages/storage` stays stdlib + SQLAlchemy + Alembic; `apps/api` is unchanged.
  - AI provider budget locked at €20–€50/mo via `CLAUDE_AI_BUDGET_MONTHLY_EUR` (default `50`); daily-morning-chain only, no on-demand AI; prompt-caching mandatory; default Claude Haiku for explanations + smaller Claude tier for forecasts.
  - Conditional order vocabulary extends to the full IBKR condition set (price + time + margin + volume + execution); TIF extends to `{DAY, GTC, OPG, IOC}`.
  - Universe-set operator-selectable via `UNIVERSE_SET` env var with three locked sets: `SP500`, `EU600`, `ALL_5K`.
  - Feedback loop + auto-weighted ensemble: ensemble combiner gains `weight_strategy=equal_weight | auto`; `auto` weights by inverse-Brier-score over rolling 90d, clipped to [0.05, 0.40] per predictor.

Out of scope (confirmed not in V1.1, deferred to a later widening):
- Multi-account portfolios.
- Mobile app.
- Real-time intraday predictor evaluation (V1.1 stays daily, scheduler-driven).
- Briefing item source distinction (portfolio / watchlist / universe_scan_candidate flow through the existing suggestion-row path).
- Real money: V1.1 still ships paper-first; the live path needs a separate scope discussion before any real-money slice.
- New predictor families beyond the V1 five (volatility-as-prediction, options-implied probs, sentiment-from-research-as-predictor, PEAD post-earnings-drift) — deferred to V1.2 once V1.1's feedback loop measures the existing five.
- Backtest persistence beyond audit rows (V1.1 keeps the backtest leaderboard computed on demand; persisted historical backtest results land in a later slice if needed).
- AI provider redundancy / fallback chain (V1.1 ships Anthropic Claude only; multi-provider routing is post-V1.1).
- Cloud deployment (V1.1 stays on the Raspberry Pi 5 + NVMe + Docker Compose target locked in `docs/deployment.md`).
