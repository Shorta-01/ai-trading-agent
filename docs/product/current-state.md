Huidige toestand: **na Task 180 — V1.1 backtesting framework (V1.1 in flight)**

- Task 180: **completed** — V1.1 backtesting framework (Slice 25 of V1.1). Pure-numpy walk-forward harness in `packages/portfolio/src/portfolio_outlook_portfolio/predictor_backtester.py`: `walk_forward_backtest(predictor, bars, *, window_days, horizon_trading_days, step_days)` slides a window of size `window_days` forward by `step_days` bars across the input series, and at each fold calls `predictor.predict(...)` on `bars[start:end]` and scores against the realised price at `bars[end + horizon_trading_days - 1]`. **Critical invariant**: a fold's predictor only ever sees `bars[start:end]` — never beyond — and the harness asserts the spy-predictor sees exactly `window_days` bars per call. Per-fold scoring computes the Brier score (`mean((predicted_prob_gain - realised_gain_indicator)²)`, lower is better, 0.25 is the random-50/50 baseline), hit-rate (predicted direction collapsed to {up, flat, down} vs realised collapsed the same way), and Sharpe ratio (realised-return mean ÷ realised-return SD, unitless, not annualised). `aggregate_window_score(...)` returns `None` for every metric when folds < `MIN_FOLDS_FOR_METRICS=2` so the leaderboard surfaces the gap honestly rather than faking a value. `run_predictor_backtest(...)` is the top-level helper that combines walk-forward + aggregation and returns `BacktestPersistenceOutputs(score, status, blocking_reason, finished_at)` with status `succeeded` / `skipped` (`insufficient_folds`); the helper never raises. Per-fold predictor exceptions are caught at the boundary; the failing fold is skipped silently. `backtest_window_score_for_predictor(...)` is the single-line plumbing every predictor wires through. **All five V1 predictors now implement `backtest_window_score(inputs, *, window_days)` → `BacktestWindowScore`**: `GbmPredictor`, `MomentumPredictor`, `MeanReversionPredictor`, `QvmFactorPredictor`, `AiTsPredictor`. New `apps/api/src/portfolio_outlook_api/predictor_backtest_orchestrator.py` module wires the harness to the storage layer: `run_backtest_for_symbol(model_code, asset_symbol, ibkr_conid, bar_repo, backtest_repo, ...)` builds the predictor (via a small dispatch table), pulls bars from `SqlAlchemyMarketDataBarRepository.list_market_data_bars_by_conid(ibkr_conid, limit=1500)`, runs the harness, and persists a running → terminal `PredictorBacktestRunRecord` audit row. `LOCKED_MODEL_CODES = frozenset({"baseline_gbm", "momentum_v1", "mean_reversion_v1"})` defines the V1.1 Slice 25 supported set; `qvm_factor_v1` returns `skipped` with stable reason `qvm_backtest_deferred_to_slice_28` (QVM needs a per-fold universe snapshot — Slice 28's QVM rebuild lifts this), `ai_ts_v1` returns `skipped` with `ai_ts_backtest_deferred_to_slice_30` (real AI provider lands in Slice 30), unknown codes return `skipped` with `unknown_model_code`, empty bars return `skipped` with `no_bars_persisted`. The orchestrator never raises — the caller always gets a `BacktestOrchestratorResult` with a persisted audit row. Two new routes: `POST /predictor/backtest/run` gated on `predictor_backtest_enabled` + writable storage; accepts body `{model_code, asset_symbol, ibkr_conid, window_days?: 252, horizon_trading_days?: 21, step_days?: 5}`; returns the serialised audit row (Decimal → string for Brier / hit_rate / sharpe). `GET /predictor/backtest/latest?model_code=&asset_symbol=&limit=25` reads from `SqlAlchemyPredictorBacktestRunRepository.list_recent_backtest_runs(...)` and returns the leaderboard surface — read-only; no row authorises an order. `serialize_backtest_run_record(...)` keeps the response payload's safety booleans hard-False. **30 new tests** (16 backtester harness covering input validation, no-look-ahead invariant via spy-predictor, blocked-predict + exception handling, aggregation edge cases, deterministic uptrend metrics, per-predictor research helper, all-five-predictors-implement-the-method; 8 orchestrator covering GBM/Momentum happy paths, QVM/AI TS skip-with-stable-reason, unknown-code, empty-bars, serialisation safety booleans, `LOCKED_MODEL_CODES` immutability; 6 endpoint covering disabled gate, missing-input, storage-not-writable, succeeded persistence, latest leaderboard with two rows including a skipped one). All 1130 tests green (611 api + 381 portfolio + 138 storage); ruff + mypy clean across all three packages. Manual approval gate stays; safety booleans hard-False on every persisted record. Slice 26 (feedback loop + auto-weighted ensemble) is now unblocked.
- Task 179: **completed** — V1.1 predictor refactor base (Slice 24 of V1.1). Activates the §22.1 heavy-dep boundary relaxation: `numpy>=2.0.0`, `pandas>=2.2.0`, `statsmodels>=0.14.0` now declared as runtime dependencies of `packages/portfolio`. New pure-numpy `_predictor_math` module centralises the primitives every predictor needs: `bar_closes_array(bars)` (Decimal closes → float64 array), `log_returns(prices)` (vectorised log returns with non-positive guards), `sample_mean(values)` (numpy mean, ddof=0), `sample_stdev(values)` (Bessel-corrected sample SD, ddof=1), `population_stdev(values)` (ddof=0 for QVM cross-sectional z-scores), `normal_cdf(z)` (scipy.stats backed), quantile constants `Z_10`/`Z_50`/`Z_90`, `decimal_from_float(value, places)` for the float→Decimal boundary, and `clipped_probability(value)` for bounded prob coercion. All four deterministic predictors (`baseline_forecast`, `momentum_predictor`, `mean_reversion_predictor`, `qvm_factor_predictor`) routed through the shared module — every existing predictor test (260+ tests across the portfolio suite) passes unchanged, proving behaviour identical. `predictor_protocol` extended with new `BacktestWindowScore(model_code, model_version, window_days, bars_used, brier_score, hit_rate, sharpe_ratio, explanation_nl)` frozen-dataclass and new `PredictorResearchProtocol(model_code, model_version, predict, backtest_window_score)` — Slice 25 (backtesting framework) wires the real implementations. Storage migration `0041_predictor_backtest_runs` adds the audit table; `PredictorBacktestRunRecord` (locked status set `{running, succeeded, failed, skipped}`, positive `window_days`, non-negative `bars_used`, `hit_rate ∈ [0, 1]` when provided, non-negative `brier_score`, safety booleans hard-False) + `SqlAlchemyPredictorBacktestRunRepository` (`save_backtest_run`, `update_backtest_run`, `list_recent_backtest_runs` with optional `model_code` + `asset_symbol` filter). Mypy ignore-list extended for `scipy.*` (no py.typed); `statsmodels.*` ignore stays. `infra/docker/.env.example` + `apps/api` settings untouched in this slice (Slice 23 already declared the env-var surface). 17 new tests cover: predictor-math primitives (12 tests: array coercion, vectorised log returns with non-positive guards, sample/population SD distinction, normal CDF, z-quantile constants, Decimal coercion, probability clipping); record-shape + repository (2 tests: roundtrip with optional metrics, invariants on status/window_days/bars_used/hit_rate/brier/safety); `BacktestWindowScore` + `PredictorResearchProtocol` (3 tests: value-object frozenness, None metrics allowed, protocol shape is structural). No predictor behaviour change in this slice; the morning chain continues to run on the V1 predictors unchanged.
- Task 178: **completed** — V1.1 doctrine amendment (Slice 23 of V1.1 — the first V1.1 slice). Docs-only PR after the V1-ship scope discussion. Amended `version-1-product-experience-locks.md` with a new §22 "V1.1 expansion lock" section: §22.1 relaxes the heavy-dependency boundary (numpy + pandas + statsmodels now allowed in `packages/portfolio`; storage stays stdlib + SQLAlchemy + Alembic; apps/api dep set unchanged); §22.2 locks the real Anthropic Claude budget at €20–€50/mo via the new `CLAUDE_AI_BUDGET_MONTHLY_EUR=50` env var (daily-morning-chain only, no on-demand, prompt-caching mandatory, default Claude Haiku for explanations + smaller Claude tier for forecasts); §22.3 extends the V1 §21.3 vocabulary with `CONDITIONAL` orders carrying the full IBKR condition set (price + time + margin + volume + execution) and extends TIF from `DAY`-only to `{DAY, GTC, OPG, IOC}`; §22.4 introduces operator-selectable `UNIVERSE_SET` (default `SP500`; allowed `SP500 | EU600 | ALL_5K`) with per-set EODHD-call caching + storage paging; §22.5 locks the feedback loop + auto-weighted ensemble (`weight_strategy=equal_weight | auto`, `auto` weights by inverse-Brier over rolling 90d clipped to [0.05, 0.40] per predictor); §22.6 carries the V1 safety doctrine intact (manual approval gate, Decimal-only money math, locked Dutch action labels, `safe_for_*=False` hard-locked, AI-is-one-vote, paper/live decided by IBKR account); §22.7 explicitly excludes multi-account / mobile / real-time intraday / briefing-source-distinction / real-money / new predictor families from V1.1 scope. New `docs/product/version-1-1-backlog.md` lists the 12-slice V1.1 queue (Slices 23–34, Tasks 178–189). New `docs/product/version-1-1-scope-register.md` tracks per-decision in-scope vs. out-of-scope. `docs/product/next-task.md` points at Slice 24 (predictor refactor base). New settings declared in `apps/api/src/portfolio_outlook_api/config.py`: `claude_ai_budget_monthly_eur` (Decimal, default 50), `universe_set` (str, default "SP500"), `predictor_backtest_enabled` (bool, default False). `packages/portfolio/pyproject.toml` adds commented-out numpy/pandas/statsmodels declarations ready for Slice 24 to activate. `infra/docker/.env.example` documents the three new V1.1 env vars. **No behavior change in this slice** — V1 morning chain continues to run on the existing predictors; Slice 24 is unblocked.
- Task 177: **completed** — V1 release readiness (Slice 22 of the V1 expansion queue, the final slice). Pure-Python `release_readiness` module in `apps/api` aggregates the storage / EODHD / IBKR / scheduler / morning-chain-leg / audit-pad settings into a Dutch scorecard with 15 stable blocker codes (`storage_not_configured`, `storage_not_writable`, `eodhd_not_configured`, `eodhd_api_key_missing`, `ibkr_not_enabled`, `ibkr_sync_not_enabled`, `scheduler_disabled`, `market_data_sync_disabled`, `forecast_sync_disabled`, `suggestions_sync_disabled`, `decision_packages_sync_disabled`, `action_drafts_sync_disabled`, `daily_briefing_sync_disabled`, `reconciliation_sync_disabled`, `prediction_diary_sync_disabled`). `compute_release_readiness(settings) -> ReleaseReadinessReport(status, summary_nl, help_nl, blockers, checks)` consumes settings via duck-typing (`getattr` only) so tests pass ad-hoc `SimpleNamespace` shapes. `ReadinessCheck(code, passed, detail_nl)` holds per-check outcome with Dutch detail strings that point at the settings flag the operator needs to flip. Constants `STATUS_READY` / `STATUS_BLOCKED`. New route `GET /v1/release-readiness` returns the serialised scorecard via `serialize_release_readiness(report)` — payload includes `status` + `summary_nl` + `help_nl` + ordered `blockers` + per-check `checks` list with hard-False `safe_for_action_drafts` / `safe_for_orders` / hard-True `blocks_orders`. End-to-end V1 acceptance test (`test_v1_acceptance.py`) drives `build_default_morning_chain_legs(...)` + `run_morning_chain(...)` with every per-leg flag on, asserts the chain returns `succeeded` with all six legs `succeeded`, asserts the release-readiness scorecard reports `ready` for the same configuration (pinning the two together as the V1 release gate), and additionally asserts the manual approval gate stays intact (safety booleans False on the morning-chain manual route response even when the chain is fully green). `docs/deployment.md` gains a "V1 release readiness" section with the required env-var checklist (`STORAGE_*`, `EODHD_*`, `IBKR_*`, `SCHEDULER_*`, the six morning-chain `*_SYNC_ENABLED` flags, the two audit-pad flags), a 5-step operator runbook (07:00 open Portefeuille → manual approval per draft → submit to paper → reconcile → poll release-readiness before production), and an explicit V1 scope-lock. `infra/docker/.env.example` consolidates every required V1 flag with commented-out placeholders for secrets (database URL, EODHD key, IBKR host/port/client_id). `version-1-backlog.md` gains a "V1 is feature-complete" banner; post-V1 widening ideas (full ~5 000-ticker universe-scan, real TimesFM / Chronos / Lag-Llama provider clients, IBKR conditional orders + GTC/OPG TIF, multi-account portfolios, mobile app, briefing item source distinction, persisted per-leg morning-chain outcomes) are documented but explicitly **out of V1 scope** until a new scope discussion with the owner. 16 new tests (9 module + 3 endpoint + 4 acceptance) cover: every-blocker-when-all-flags-off, all-flags-on yields ready with no blockers, EODHD key missing surfaces independently, scheduler-disabled is one blocker, morning-chain flags surface independently, audit-pad flags surface independently, missing `storage` attribute yields exactly one storage blocker, ready report safety booleans, blocked report blocker count + safety booleans, endpoint blocked by default / ready when every flag set / one-flag-off shows exactly that blocker, full morning-chain acceptance, release-readiness ready under the same configuration, endpoint surfaces ready status, safety booleans never flip even when ready. No new persisted records; no migration. Manual approval gate stays; safety booleans hard-False everywhere. The V1 expansion queue (Slices 13–22) is now closed.
- Task 176: **completed** — Scheduler-driven 07:00 morning chain (Slice 21 of the V1 expansion queue, §21.7 locked). New pure-Python `morning_chain` orchestrator in `apps/api` composes the six-leg V1 daily chain — market-data sync → ensemble forecast → asset suggestions → Decision Packages → action drafts → daily briefing — into a single sequenced runner. The orchestrator is generic: `run_morning_chain(*, legs: Sequence[LegCallable]) -> MorningChainResult` iterates each leg in order, treats `skipped` outcomes as "continue" (leg disabled in config), `failed` outcomes as "stop", and translates raised exceptions into a stable `leg_callable_raised` failure code so the scheduler audit chain never crashes. `MorningChainLegOutcome(leg_name, status, failure_code, detail_nl)` is the per-leg shape with status set `{succeeded, skipped, failed}`; `MorningChainResult(status, failed_leg, failure_code, started_at, completed_at, legs)` is the aggregated outcome. Stable canonical leg names locked in `MORNING_CHAIN_LEG_NAMES = (market_data_sync, forecast_sync, suggestion_sync, decision_package_sync, action_draft_sync, daily_briefing_sync)`; leg-outcome construction rejects unknown leg names + invalid statuses + failed-without-failure-code. `MorningChainFailed(failed_leg, failure_code, detail_nl)` exception is raised by the scheduler-job wrapper so the existing `run_daily_briefing_job(...)` from Slice 13 captures the failure on the `SchedulerRunRecord.error_text` field. `build_scheduler_chain_callable(legs_factory)` produces the no-arg callable that backs the scheduler job; `legs_factory` runs once per fire so each invocation rebuilds short-lived sessions. `build_default_morning_chain_legs(settings)` constructs the six production leg adapters — each leg respects its own `<x>_sync_enabled` flag (`market_data_sync_enabled`, `forecast_sync_enabled`, `suggestions_sync_enabled`, `decision_packages_sync_enabled`, `action_drafts_sync_enabled`, `daily_briefing_sync_enabled`) so the chain can be opted into incrementally; when a flag is off the corresponding leg returns `skipped` with a Dutch detail string pointing at the setting name. `install_default_jobs(...)` now defaults the registered job's callable to the morning-chain runner (lazy import keeps the scheduler module dependency-free of the orchestrator); when the operator does inject `job_callable=...` the legacy override still works (used by tests). New route `POST /scheduler/runs/morning-chain` exercises the chain on demand for release-readiness smoke testing: gated on `scheduler_enabled` + writable storage; builds the leg set from settings; persists an initial `SchedulerRunRecord` with `triggered_by="manual"`, status `running`; runs the chain catching `MorningChainFailed`; updates the audit row with the terminal status and (on failure) `error_text=f"{failed_leg}:{failure_code}"`; returns the full structured per-leg outcome plus the `run_id` in the response body. Storage layer untouched — the existing `scheduler_runs` table from Slice 13 absorbs both scheduler-triggered and manual rows. Manual approval gate stays; safety booleans hard-False on every persisted record and every response. 30 new tests (15 morning-chain orchestrator + 11 scheduler-endpoint + 4 scheduler-default-callable) cover: dataclass invariants (3), all-succeed/stop-on-first-failure/skipped-doesn't-stop/exception-catching (4), scheduler-callable returns-None-on-success and raises-MorningChainFailed-on-failure (2), default-leg factory all-skipped/all-succeed/partial-enable (3), serialisation safety-booleans + failed-leg surfacing (2), endpoint blocked-when-scheduler-disabled / blocked-when-storage-not-writable / runs-and-persists / reports-failed-leg-in-audit-row (4), and scheduler-default-callable wires the morning chain (2). The full briefing-source distinction (portfolio / watchlist / universe_scan_candidate) and the production wire-up of each leg adapter to its underlying sync function are deferred to Slice 22 / a post-V1 widening — the chain already feeds the existing suggestion + briefing pipeline so universe-scan candidates flow through the existing suggestion-row path; Slice 22's release readiness slice will exercise the chain end-to-end with stub providers.
- Task 175: **completed** — Full IBKR order vocabulary (Slice 20 of the V1 expansion queue, §21.3 locked). Storage migration `0040_action_draft_order_vocabulary` adds five nullable Decimal columns to `asset_action_drafts`: `stop_price`, `trail_amount`, `trail_percent`, `bracket_take_profit_limit_price`, `bracket_stop_loss_price`. `AssetActionDraftRecord` extended with the five new optional fields plus a `LOCKED_ORDER_TYPES = frozenset({"LMT", "MKT", "STP", "STP_LMT", "TRAIL", "TRAIL_LMT", "BRACKET"})` constant; `__post_init__` now rejects any `order_type` outside the locked set and delegates per-type field requirements to `_enforce_order_type_invariants(record)`. Per-type rules: LMT requires `limit_price > 0`; MKT has no extras; STP requires `stop_price > 0`; STP_LMT requires both `stop_price > 0` and `limit_price > 0`; TRAIL/TRAIL_LMT require exactly one of `trail_amount > 0` or `trail_percent > 0` (and TRAIL_LMT also `limit_price > 0`); BRACKET requires `limit_price + bracket_take_profit_limit_price + bracket_stop_loss_price` all positive and correctly ordered for the action side (BUY: tp > limit > sl; SELL: tp < limit < sl). Negative values on any of the five new fields raise "must be positive when provided". `action_draft_safety.run_dry_run_safety_checks(...)` mirrors the per-type validation with stable failure codes: `unsupported_order_type`, `stp_missing_stop_price`, `stp_lmt_missing_stop_or_limit`, `trail_missing_trail_value`, `trail_amount_and_percent_set`, `trail_lmt_missing_limit_price`, `bracket_missing_take_profit`, `bracket_missing_stop_loss`, `bracket_take_profit_below_limit`, `bracket_stop_loss_above_limit`, `bracket_inverted_for_sell`. `invalid_limit_price` now only fires for order types that need a limit (LMT / STP_LMT / TRAIL_LMT / BRACKET). `DraftSizing` extended with the same optional fields + `order_type` default "LMT" so existing callers stay unchanged. **`IbapiOrderSubmissionClient` gets the full vocabulary mapping** via a new `build_contract_and_orders(inputs) -> (contract, list[Order])` builder that returns one order for non-BRACKET types and three orders (parent + take-profit + stop-loss) for BRACKET. `OrderSubmissionInputs` gains the five new optional fields plus `order_type` (default "LMT"). Per-type mapping: LMT → `orderType="LMT"` + `lmtPrice`; MKT → `orderType="MKT"`; STP → `orderType="STP"` + `auxPrice=stop_price`; STP_LMT → `orderType="STP LMT"` + `auxPrice=stop_price` + `lmtPrice=limit_price`; TRAIL → `orderType="TRAIL"` with `auxPrice=trail_amount` XOR `trailingPercent=trail_percent`; TRAIL_LMT → `orderType="TRAIL LIMIT"` + trail field + `lmtPrice=limit_price`; BRACKET → parent (`orderType="LMT"`, `lmtPrice=limit_price`, `transmit=False`) + take-profit child (opposite-side `orderType="LMT"` with `lmtPrice=tp`, `transmit=False`) + stop-loss child (opposite-side `orderType="STP"` with `auxPrice=sl`, `transmit=True`). The submission flow places three orders on `parent_id`, `parent_id + 1`, `parent_id + 2` after patching the children's `parentId = parent_id`, then records the parent's `order_id` / `perm_id` on the result. `submit_action_draft_to_paper(...)` threads the new fields from `AssetActionDraftRecord` into `OrderSubmissionInputs`. The legacy `build_contract_and_order(...)` is retained for single-order callers and raises when fed BRACKET (use `build_contract_and_orders` instead). Tests added: 11 storage record tests covering all 7 order types' happy paths + every per-type failure mode + negative-value rejection on the new fields; 13 dry-run tests covering each new failure code + bracket happy path; 14 submission-client tests covering the LMT/MKT/STP/STP_LMT/TRAIL/TRAIL_LMT mapping, BRACKET parent+children flag/action wiring (BUY and SELL), `build_contract_and_order` BRACKET rejection, unsupported-order-type rejection, and the three-orders-with-chained-parentId placement loop. Locked safety doctrine intact: per-draft manual approval gate stays; `safe_for_*=False` on every persisted record; no UI change in this slice; no new routes. Approval orchestrator + state machine unchanged.
- Task 174: **completed** — Fractional Kelly + risk-parity sizing on the BUY path (Slice 19 of the V1 expansion queue, §21.5 locked). New pure-Python `kelly_sizing` module in `packages/portfolio` with `compute_fractional_kelly_fraction(*, prob_gain, expected_return_pct, downside_loss_pct, kelly_fraction=0.5)` implementing the asymmetric Kelly formula `(p × b − q × L) / (b × L)` clipped to `[0, 1]` then multiplied by the fractional-Kelly coefficient (half-Kelly default). Returns `0` on any of: negative EV, zero downside, zero prob_gain, non-positive kelly_fraction — the doctrine prefers "do nothing" over "size a noisy edge". `apply_risk_parity_caps(*, fraction, current_sector_exposure_pct, per_asset_cap_pct=5.0, per_sector_cap_pct=30.0)` clips to the locked per-asset (5%) and per-sector (30% minus current sector exposure) caps; emits a `KellyResult` with `fraction_raw_kelly`, `per_asset_cap_hit`, `per_sector_cap_hit`, and a Dutch explanation describing which cap (if any) fired. Companion `size_buy_with_kelly(...)` end-to-end helper returns `(quantity, KellyResult)` after multiplying the capped fraction by `usable_cash` and flooring to whole shares. Extended `DraftSourceContext` with five new optional fields: `ensemble_prob_gain`, `ensemble_expected_return_pct`, `ensemble_downside_loss_pct`, `current_sector_exposure_pct`, `current_portfolio_position_count`. When all four numerical Kelly inputs are present *and* the portfolio + cash values are positive, the `Kopen` BUY path now sizes as `floor(min(capped_kelly_fraction × portfolio_value, cash_amount) / market_price)`. When any input is missing the path **falls back to the legacy default-buy-value sizing** rather than guessing — no behaviour change for existing tests. `Langzaam bijkopen` / `Verminderen` / `Verkopen` paths keep their existing top-up / reduce / sell-all semantics; Kelly only changes the cold-start `Kopen` path. 29 new tests (22 Kelly module + 7 BUY-path) cover: classic symmetric Kelly, asymmetric positive EV, modest edge yields modest fraction, negative-EV-returns-zero, zero-expected-return, zero-downside, zero-kelly-fraction, full-vs-half scaling, default Kelly fraction = ½, no-cap-below-per-asset, per-asset cap clip, per-sector cap clip, both caps active, sector-already-full → zero, unknown-sector-skip, explanation-describes-active-cap, default caps locked at 5/30, end-to-end shares, fraction-collapses-to-zero, zero-cash → zero, zero-price → zero, sector cap reduces qty vs no-sector, BUY-Kelly happy path, fallback-when-ensemble-missing, negative-EV → blocked, cash-smaller-than-target-shrinks-capital, sector-cap-reduces-qty, fallback-when-portfolio-value-missing, fallback-when-cash-missing. Locked safety doctrine intact; no broker action; no orchestrator wiring yet (Decision Package + decision_package_sync integration lands in Slice 20+).
- Task 173: **completed** — AI foundation TS-model predictor (Slice 18 of the V1 expansion queue, fifth and final predictor of §21.4). Two-package implementation mirroring Slice 10's explanation provider pattern. **Packages/portfolio side**: new `ai_ts_predictor` module defines the locked input/output contract — `TsModelProviderInputs(historical_bars, current_price, horizon_trading_days, asset_symbol, sector)`, `TsModelProviderResult(p10/p50/p90, prob_gain, expected_return_pct, confidence_score, model_provider_code, model_name, model_version, explanation_nl)`, `TsModelProviderProtocol(forecast(inputs))`, and `TsModelProviderUnavailable(reason, detail_nl)` sentinel. New pure-Python `AiTsPredictor` implements `PredictorProtocol`: receives an injected provider, validates the result (monotonic quantiles p10 ≤ p50 ≤ p90, prob_gain ∈ [0, 1], confidence ∈ [0, 1]), and returns a `PredictionDistribution`. Locked blocking reasons: `invalid_horizon`, `invalid_current_price`, `provider_unavailable`, `provider_error`, `invalid_quantiles`, `invalid_prob_gain`, `invalid_confidence`. When the injected provider is `TsModelProviderUnavailable` the predictor returns a clean blocked distribution so the ensemble combiner drops the AI vote without crashing. Provider exceptions are caught at the boundary and surfaced as `provider_error`. **Apps/api side**: new `ai_ts_provider` module with `StubTsModelProvider` (deterministic Python: log-returns → empirical drift + volatility → lognormal distribution; falls back to neutral when bars/prices are insufficient) and `build_ts_model_provider(settings)` factory. Factory gates: returns the stub only when `ai_ts_predictor_enabled=True` and `ai_ts_predictor_provider_code="stub"`; any other code returns `TsModelProviderUnavailable("real_client_not_implemented")` — real TimesFM / Chronos / Lag-Llama providers land in a future widening slice. New settings `ai_ts_predictor_enabled` (default False), `ai_ts_predictor_real_client_enabled` (default False), `ai_ts_predictor_provider_code` (default `"stub"`). 25 new tests cover predictor invariants (14: happy path / explanation includes provider identity / direction mapping / prob_loss derivation / all blocking paths / determinism / model identity / metadata forwarding) and provider behaviour (11: factory gates / monotone quantiles / short-history fallback / zero-price fallback / drift sign on up/down trends / determinism / explanation shape). The AI predictor is now drop-in interchangeable with GBM, Momentum, Mean-Reversion, and QVM through `PredictorProtocol` — Slice 19+ composes all five in `forecast_sync`.
- Task 172: **completed** — Universe scan orchestrator + registry + routes (Slice 17 of the V1 expansion queue, the wiring layer that activates §21.6). Added `universe_registry` module in `apps/api` listing the V1 locked universe — Bel20 (20), AEX (25), CAC40 (40), DAX40 (40), S&P 100 (~100) and a NASDAQ100-extra set (~65 non-overlapping tech names); each entry carries `symbol`, `eodhd_symbol`, `index_code` and a best-effort `sector`. `locked_universe()` deduplicates by `eodhd_symbol` (HON, ASML and a few others appear in multiple indices). New storage migration `0039_universe_scan_runs` + `UniverseScanRunRecord` (locked status set `running/succeeded/failed/skipped`, locked `triggered_by` set `scheduler/manual`, non-negative counts, safety booleans hard-False) + `SqlAlchemyUniverseScanRunRepository` (save / update / latest / list). New `universe_scan_sync` orchestrator: iterates the locked universe (capped at `universe_scan_max_tickers_per_run`), per ticker calls `EodhdClient.fetch_fundamentals(...)` then `fetch_eod_bars(...)`, computes 6m/12m returns from bars when the fundamentals payload omits them, and persists an `AssetFundamentalsSnapshotRecord`. Per-ticker failures are recorded but never abort the batch; persistence errors are classified as failed. The scan_run row is written as `running` first then updated to a terminal state so the audit chain shows in-progress state. Companion `build_universe_fundamentals(snapshots)` helper translates persisted snapshots into the QVM predictor's `UniverseFundamentals` shape — Slice 18+ wires the predictor invocation. New routes `POST /universe/scan/run` (gated five ways: `universe_scan_sync_enabled` + EODHD enabled + EODHD key + writable storage + storage reachable) and `GET /universe/scan/runs/latest`. New settings `universe_scan_sync_enabled` (default False), `universe_scan_max_tickers_per_run` (default 50), `universe_scan_history_lookback_days` (default 400). 23 new tests cover: registry (each index meets minimum size, dedup, all indices covered, by-index filter, well-formed symbols, frozen-dataclass invariant), orchestrator (happy path / cap iteration / fundamentals-fetch-error / bars-fetch-error / persistence-error / empty batch / 6m+12m derivation from bars / fundamentals-returns-take-precedence / ranked count skips empty factors / build_universe_fundamentals helper), endpoint paths (sync disabled / EODHD not configured / storage not writable / happy with stubbed orchestrator / latest not-configured / not-found / found), and storage (roundtrip + invariants). Disabled-by-default; safety booleans hard-False on every record and response; no broker action.
- Task 171: **completed** — QVM factor predictor + fundamentals fetcher + storage (Slice 16 of the V1 expansion queue, third step of §21.4). Storage migration `0038_asset_fundamentals_snapshots` adds the `asset_fundamentals_snapshots` table (one row per `(eodhd_symbol, fetched_at)` via UNIQUE; carries `pe_ratio`, `pb_ratio`, `ev_ebitda`, `roic_pct`, `gross_margin_pct`, `dividend_yield_pct`, `return_6m_pct`, `return_12m_pct`, `market_cap`, `sector`, `currency`, `raw_payload_hash`, `provider_code`; safety booleans hard-False). New `AssetFundamentalsSnapshotRecord` dataclass + `SqlAlchemyAssetFundamentalsSnapshotRepository` with `save_snapshot`, `get_latest_snapshot_for_symbol`, and `list_latest_universe_snapshots` (group-by-symbol + max-fetched-at via subquery join that works on SQLite + Postgres). Extended `EodhdClient` with `fetch_fundamentals(eodhd_symbol)` hitting `/fundamentals/{SYMBOL.EX}`; new `EodhdFundamentals` dataclass + `_parse_fundamentals` reads from `General`, `Highlights`, `Valuation`, `Technicals` sections of EODHD's nested payload; locale-aware `_percent_or_none` treats ratios ≤ 1.5 as fractions and multiplies by 100. New pure-Python `QvmFactorPredictor` in `packages/portfolio` implementing `PredictorProtocol`: Quality = z-score of (ROIC + gross_margin); Value = z-score of inverted P/E + P/B + EV/EBITDA (each clipped to a sane range first — P/E [1, 80], P/B [0.1, 25], EV/EBITDA [1, 60]); Momentum = z-score of (6m + 12m return). Composite z = average of available factor z-scores (predictor uses whatever factors are present rather than blocking on missing data per asset). Composite is soft-clipped at ±2 to [-1, +1] then mapped to ±25% annualised drift; trailing 6-month volatility scales the lognormal distribution. Universe is injected as a pure-Python `UniverseFundamentals(entries=tuple[FundamentalsEntry, ...])` fixture in this slice — Slice 17 will wire the daily scan that populates it from EODHD. Locked blocking reasons: `invalid_horizon`, `invalid_current_price`, `insufficient_history` (< 130 bars), `insufficient_universe` (< 5 entries), `symbol_not_in_universe`, `insufficient_factors` (target has no factor components), `flat_history`. Uniform universes (zero spread on a factor) yield a z-score of 0 — the factor genuinely carries no signal cross-sectionally, so the target's relative score is neutral. Tests cover the QVM math against synthetic universes (strong / weak / neutral score, missing-factor degradation, deterministic) and every blocking path. No orchestrator change yet; the QVM predictor joins the ensemble once Slice 17 wires the universe scan into `forecast_sync`.
- Task 170: **completed** — Mean-reversion predictor + ensemble combiner (Slice 15 of the V1 expansion queue). Added `MeanReversionPredictor` in `packages/portfolio` (pure Python): combines three classical mean-reversion signals — RSI (Relative Strength Index, 14-day, Wilder smoothing); Bollinger position (z-score against 20-day SMA, capped at ±3); Hurst exponent (rescaled-range fit over the trailing 100-day window using chunk sizes 10/20/50/100). Composite pull factor = average of RSI + Bollinger strength scaled by Hurst confidence (1 - 2H, clamped to [0, 1]); when the series is trending the pull collapses toward zero. Projected price = `current + pull × (SMA_20 - current)` (price reverts toward the moving average); distribution width comes from trailing 6-month volatility. Locked blocking reasons: `invalid_horizon`, `invalid_current_price`, `insufficient_history` (< 130 bars), `flat_history` (zero recent volatility). New `ensemble_combiner` in `packages/portfolio`: `compute_ensemble_forecast(predictors, inputs, weights)` runs every predictor, drops `blocked` results, and combines surviving distributions via weighted averages of p10/p50/p90, expected_return_pct, prob_gain/loss and confidence; direction is re-derived from combined expected return; confidence is multiplied by an `agreement_factor` (1.0 on full agreement, 0.6 on partial mix, 0.4 on positive-vs-negative split). Weights default to equal-weight; the caller can pass `weights: dict[str, Decimal]` (model_code → weight) which is normalised over surviving predictors; missing entries default to 1.0; non-positive weights fall back to 1.0; zero total weight is reported as a blocked ensemble. Returns `EnsembleResult(forecast, contributions, blocked_model_codes)`; per-predictor `EnsembleContribution(model_code, model_version, weight_raw, weight_normalised, prediction)` exposes the breakdown for downstream Diary tracking (wired in a later slice). Pure Python, deterministic, no orchestrator change yet — Slice 17 wires the ensemble into `forecast_sync`. 27 new tests cover the mean-reversion paths (insufficient / invalid horizon / invalid price / flat / downside-from-spike / upside-from-dip / dampened-on-trend / flat-at-SMA / deterministic / explanation shape / identity) and the combiner (empty / all-blocked / blocked-excluded / equal-weight / custom-weights / missing-weight-default / zero-weight / agreement-full / agreement-split / direction-derivation / real-predictors-on-uptrend / deterministic / all-block-real-predictors / explanation-lists-contributors / p10-p50-p90 weighted averages / strong-down propagation).
- Task 169: **completed** — `PredictorProtocol` + Momentum predictor (Slice 14 of the V1 expansion queue, first concrete step of §21.4). Added pure-Python `predictor_protocol` in `packages/portfolio` with the locked input/output contract: `PredictorInputs(historical_bars, current_price, horizon_trading_days, asset_metadata)` and `PredictionDistribution(model_code, model_version, horizon_trading_days, current_price, p10/p50/p90_price, prob_gain/loss, expected_return_pct, direction, confidence_score, status, blocking_reason, explanation_nl)` — `__post_init__` rejects blank model_code/version, non-positive horizon, unknown direction (locked set: strong_up / slight_up / flat / slight_down / strong_down), and unknown status (ready / blocked). Wrote `GbmPredictor` adapter around the existing `compute_baseline_forecast` so the GBM math is unchanged but exposed via the protocol; the GBM "neutral" direction maps to the protocol's "flat" label, and blocked forecasts default to `flat` so the combiner can ignore them cleanly. New `MomentumPredictor` (deterministic Python, no SciPy): composite score = average of (a) 12-1 momentum (12-month log-return excluding the most-recent month, normalised by `log(1.25)`) and (b) time-series momentum (6-month log-return ÷ (6-month SD × √horizon)); both clipped to [-1, +1]. Score × `MAX_ANNUAL_DRIFT_PCT` (25%) gives annualised drift; trailing 6-month volatility scales the lognormal distribution; same direction thresholds and confidence heuristic as GBM. Deterministic blocking reasons: invalid_horizon, invalid_current_price, insufficient_history (< 250 bars), flat_history (no recent volatility). Tests cover the protocol invariants, GBM wrapping (ready / insufficient_history / invalid_current_price), and Momentum (insufficient / invalid horizon / invalid price / flat-recent / upside-on-uptrend / downside-on-downtrend / sideways-noise / drop-in-shape with GBM / deterministic / explanation-shape / model identity). No orchestrator change yet — the combiner lands in Slice 15. APScheduler unchanged; no new routes; no storage; no UI. Safety doctrine untouched.
- Task 168: **completed** — doctrine relock + APScheduler skeleton (Slice 13 of the V1 expansion queue). Relaxes three previously-blocking app-side gates per the §21.1 doctrine relock: the `account_mode_mismatch` dry-run failure (removed from `run_dry_run_safety_checks`); the `version1_paper_only` / `account_mode_mismatch` blockers in `ibkr_sync_readiness`; the `ibkr_expected_environment` and `ibkr_sync_account_mode` gates in `build_real_order_submission_client`. The connected IBKR account now decides paper vs. live; the runtime is identical for both. New `GET /ibkr/account/mode` endpoint reports the detected mode for the dashboard badge (PAPER / LIVE / UNKNOWN) — never authorises an order. Storage migration `0037_scheduler_runs` adds the `scheduler_runs` audit table with `SchedulerRunRecord` (locked status set `running/succeeded/failed/skipped`, locked `triggered_by` set `scheduler/manual`, safety booleans hard-False) and `SqlAlchemySchedulerRunRepository`. New `scheduler` module wires `apscheduler.BackgroundScheduler` into the FastAPI lifespan (started only when `scheduler_enabled=True`); the daily_briefing job is a skeleton callable for V1 — Slice 21 will replace it with the full morning chain. `run_daily_briefing_job(...)` captures every fire as a `SchedulerRunRecord` with running → succeeded/failed transitions; persistence errors never crash the scheduler. Two new routes: `GET /scheduler/jobs` (lists registered jobs + next-fire) and `GET /scheduler/runs/latest` (most recent audit row, optionally filtered by job_name). New settings `scheduler_enabled` (default `False`), `scheduler_timezone` (default `Europe/Brussels`), `scheduler_daily_briefing_cron` (default `30 6 * * *`). Web: dashboard `PAPER` / `LIVE` badge in the Portefeuille panel header (info-tone for paper, warning-tone for live, muted for unknown); a new "Scheduler" panel shows status, timezone, cron, next-fire and last-run summary. APScheduler added as an apps/api dependency with mypy `ignore_missing_imports`. Disabled-by-default; manual approval gate stays; safety booleans hard-False on every persisted record and every API response. No new predictors yet — this slice clears the runway for slices 14–22.
- Task 167: **completed** — deterministic daily briefing + alert digest end-to-end (Slice 12 of the V1 critical path). Added storage migration `0036_daily_briefings` with two new tables: `daily_briefings` (one row per `briefing_date` via UNIQUE constraint, carries counts + Dutch summary + safety booleans hard-False) and `briefing_alerts` (append-only items the briefing references). Added `DailyBriefingRecord` + `BriefingAlertRecord` dataclasses with non-negative-counts and severity (`info`/`warning`/`critical`) invariants. Added `SqlAlchemyDailyBriefingRepository` with upsert-by-date + latest + alert save/list/delete. New pure-Python `daily_briefing` module in `packages/portfolio` with typed `BriefingInputs` (positions, suggestions, packages, drafts, diary outcomes, critical events) and `compute_daily_briefing(inputs)` deterministic builder. Rules: only items at-or-after `lookback_started_at` are counted; new suggestions/packages/drafts each emit an `info` alert (failed-dry-run draft escalates to `warning`); closed diary outcomes with a non-null 1m label emit an `info` alert; critical state events emit a `critical` alert; FX status `stale` emits a `warning` alert; the Dutch summary references the locked counts directly. AI never authors the briefing. New `daily_briefing_sync.generate_daily_briefing(...)` orchestrator (apps/api) reads positions + cash + suggestions + packages + drafts + diary entries + critical events from the existing repositories, sums cash by currency, runs the deterministic builder, and persists the briefing + its alerts. New routes `POST /briefings/daily/compute` (gated five ways) and `GET /briefings/daily/latest` (returns the briefing + its alerts). New settings `daily_briefing_sync_enabled` (default False) and `daily_briefing_lookback_hours` (default 24). Portefeuille page renders a new "Dagbriefing" panel above the positions table, with "Genereer briefing" + "Vernieuw" buttons and a coloured alert list (warning + critical severities highlighted). Disabled-by-default; `safe_for_action_drafts` + `safe_for_orders` stay hard-False on every persisted row and every API response; the briefing never auto-promotes into a draft or order.
- Task 166: **completed** — Belgian tax module (TOB + dividend roerende voorheffing) wired into the action draft Orderimpact preview (Slice 11 of the V1 critical path). New pure-Python `belgian_tax` module in `packages/portfolio` with locked rates and per-transaction caps (2025 schedule): `TOB_RATE_BOND=0.12%` (cap €1 300), `TOB_RATE_STANDARD=0.35%` (cap €1 600), `TOB_RATE_ACCUMULATING=1.32%` (cap €4 000), `BELGIAN_DIVIDEND_WITHHOLDING_RATE=0.30`. `TobSecurityClass` StrEnum (`standard_stock`, `distributing_etf`, `accumulating_etf`, `bond`, `sicav_redemption`, `other`) — the string values land on the persisted action-draft row so the audit chain proves which rate was applied. `compute_tob(*, transaction_value, security_class)` returns the rounded-HALF_UP-to-cents amount, capped per class; zero returns zero; negative inputs raise `ValueError`; non-Decimal raises `TypeError`. `compute_dividend_withholding(*, gross_dividend)` returns `gross * 30%` with the same Decimal-only contract. Both helpers are pure Python — no I/O, no datetime, Decimal-only. Extended `Orderimpact` dataclass in `packages/portfolio/action_draft_safety` with `estimated_belgian_tob: Decimal` and `belgian_tob_security_class: str`; `compute_orderimpact(..., belgian_tob_security_class=TobSecurityClass.STANDARD_STOCK)` computes the TOB on `estimated_order_value` and threads both fields through. Storage migration `0035_action_draft_belgian_tob` adds two nullable columns (`estimated_belgian_tob`, `belgian_tob_security_class`) to `asset_action_drafts`; `AssetActionDraftRecord` gains both as `Decimal | None` / `str | None` defaults and `__post_init__` rejects a negative TOB. The `action_draft_sync.generate_action_drafts(...)` orchestrator passes the new fields into the persisted record; `serialize_action_draft_for_response` surfaces them on `GET /action-drafts/latest` + `GET /action-drafts/{draft_id}`. Portefeuille Action drafts table grows a "TOB (BE)" column with the security-class on hover. **Informational only** — the TOB does not change order sizing, does not promote a draft to a submission, and does not lift any safety boolean. Safety booleans (`safe_for_submission` / `safe_for_orders` / `safe_for_broker_submission`) remain hard-False on every persisted row and every response. No broker execution; no AI authoring; Decimal-only math.
- Task 165: **completed** — AI explanation layer (RAG read-only) end-to-end (Slice 10 of the V1 critical path). Added storage migration `0034_decision_package_explanations` with two new tables: `decision_package_explanations` (one record per `(decision_package_id, decision_package_content_hash)` pair via UNIQUE constraint; carries model identity, input/output hashes, Dutch explanation, locked risk disclaimer, status, blocking_reason and hallucinated_numbers_json; safety booleans `safe_for_self_learning` / `safe_for_action_drafts` / `safe_for_orders` default-False and dataclass rejects True) and `explanation_evidence_ledger` (append-only audit: one row per evidence item the model saw, kind + reference id + content_hash). Added `DecisionPackageExplanationRecord` + `ExplanationEvidenceLedgerRecord` dataclasses and `SqlAlchemyDecisionPackageExplanationRepository` (save + latest-per-package + per-version lookup + ledger save + ledger list). Pure-Python `ai_explanation_guards` in `packages/portfolio`: `validate_explanation_output(...)` enforces the V1 doctrine — output must (a) be non-empty, (b) fit within `max_output_chars`, (c) contain the locked Dutch risk disclaimer verbatim, (d) NEVER introduce a numeric token that is not in the input. A locale-aware numeric tokenizer normalises thousand-separators and Dutch decimal commas so legitimate formatting changes don't trip the guard. Status outcomes are `generated` or `blocked` with deterministic reasons (`empty_output`, `output_too_long`, `disclaimer_missing`, `hallucinated_numbers`). New `ai_explanation_provider` in `apps/api`: `ExplanationProviderInputs` + `ExplanationProviderResult` + `ExplanationProviderProtocol` boundary; `StubExplanationProvider` deterministically paraphrases the Decision Package's already-persisted `rationale_nl` + `explanation_nl` + research snippet and appends the locked disclaimer (no AI runtime, no network); `build_explanation_provider(settings)` returns the stub only when `ai_explanation_enabled=True` and `ai_explanation_provider_code="stub"`. Real providers (Anthropic / OpenAI) return `ExplanationProviderUnavailable(reason="real_client_not_implemented")` — the boundary is wired so a future slice can plug them in without touching the orchestrator. New `ai_explanation_sync.generate_explanation(...)` orchestrator: builds a canonical JSON of the package + linked research, hashes it as the input-evidence anchor, calls the provider, runs the validator, persists the explanation row plus one ledger entry per evidence source (`decision_package` + one `research_source` per linked source). Provider exceptions and persistence errors are classified as `failed` without poisoning the diary. New routes `POST /decision-packages/{decision_package_id}/explanation` (gated five ways: `ai_explanation_enabled`, writable storage, package exists, provider available, output validates) and `GET /decision-packages/{decision_package_id}/explanation` (read-only latest). Added settings `ai_explanation_enabled` (default `False`), `ai_explanation_real_client_enabled` (default `False`), `ai_explanation_provider_code` (default `"stub"`), `ai_explanation_max_output_chars` (default `2000`). Portefeuille page renders an AI uitleg subsection per Decision Package card with "Genereer / vernieuw" + "Lees laatste" buttons, provider/model identity footer, blocked-reason warning row and full Dutch explanation text. Disabled-by-default; `safe_for_self_learning`, `safe_for_action_drafts`, `safe_for_orders` remain hard-False on every persisted explanation row and every API response; AI never originates a number; the locked risk disclaimer is always appended.
- Task 164: **completed** — Research Desk runtime wired into the Decision Package evidence chain (Slice 9 of the V1 critical path). Added storage migration `0033_decision_package_research_evidence` extending `asset_decision_packages` with five new nullable columns (`research_evidence_count` defaulting to 0, `research_credibility_summary`, `research_freshness_status`, `research_blocking_reason`, `research_snippet_nl`) — read-only context that **never** lifts a block. Underlying invariants on `ResearchSourceEvidenceItemRecord` and `ResearchSourceCredibilityAssessmentRecord` keep `safe_to_use_for_suggestions=False` and `blocks_suggestions=True` (storage doctrine). Extended `AssetDecisionPackageRecord` with the five fields and added a `research_evidence_count >= 0` invariant in `__post_init__`. New pure-Python `research_evidence_summary` in `packages/portfolio`: `ResearchEvidenceInputs` + `ResearchEvidenceSummary` + `summarize_research_for_asset(sources, *, now)`. Deterministic rules: aggregate credibility (`high` only when all sources are high; `low` only when all are low; otherwise `mixed`; `no_research` when count==0); freshness from per-source max-recent-signal timestamp vs `now` (fresh ≤30d, stale >90d, mixed in between); blocking_reason becomes `prompt_injection_high_risk` if any source has high prompt-injection risk and `credibility_rejected` if any source has `rejected` credibility (prompt-injection block takes precedence). AI never appears here; all rules are explicit. New `build_research_summary_by_symbol(symbols, *, research_repo, now)` helper in `decision_package_sync` that pulls every research source linked to each asset symbol via the existing `SqlAlchemyResearchSourceArchiveRepository.list_research_sources_for_asset(...)`, fetches the latest credibility-assessment and prompt-injection-scan per source, derives the most-recent signal timestamp, and feeds the deterministic summarizer. The `sync_decision_packages(...)` orchestrator now accepts a `research_summary_by_symbol` kwarg; the `_AssemblyContext` and the immutable record both carry the research summary through. The content_hash now includes the four scalar research fields (count + credibility + freshness + blocking_reason) so package versioning correctly reflects research changes. `POST /decision-packages/compute` builds the per-symbol summary just before calling the orchestrator. `GET /decision-packages/latest` and `GET /decision-packages/{conid}/latest` now surface the five research fields via the updated `serialize_decision_package_for_response`. Portefeuille page renders a Research subsection on every Decision Package card (count + credibility + freshness + Dutch snippet, with a highlighted warning row when `research_blocking_reason` is set). Disabled-by-default at the existing `decision_packages_sync_enabled` gate; safety booleans (`safe_for_action_drafts`, `safe_for_orders`, `safe_for_broker_submission`, `actions_allowed`, `order_*_allowed`, `blocks_orders`) remain hard-False everywhere. No new routes; no AI authoring; research evidence can block but never auto-promote.
- Task 163: **completed** — reconciliation + Prediction Diary end-to-end (Slice 8 of the V1 critical path). Added storage migration `0032_prediction_diary_entries` with `prediction_diary_entries` table (UNIQUE on `suggestion_id`, with `safe_for_self_learning` / `safe_for_model_retraining` defaulting to False) + matching SQLAlchemy table, `PredictionDiaryEntryRecord` dataclass (issued forecast snapshot, three horizon-locked realised prices/returns/outcomes 1d/1w/1m, dataclass `__post_init__` rejects safety booleans True), and `SqlAlchemyPredictionDiaryRepository` (upsert-by-suggestion-id + list). Added pure-Python `prediction_diary_eval` in `packages/portfolio`: `evaluate_diary_outcomes(...)` returning deterministic outcome labels (`right` / `wrong` / `early` / `inconclusive` / `no_data`) per horizon — rule is locked: `|return_pct| ≤ tolerance → inconclusive`; direction missed → `wrong`; direction matched + realised exceeds p50 in the expected direction → `right`; otherwise → `early`; missing/zero realised price → `no_data`. AI **never** assigns the label. New `reconciliation_sync.reconcile_submissions(...)` orchestrator drives submissions in `AWAITING_IBKR_REPLY` / `REPLY_CONFIRMED` / `WORKING` to their terminal state by matching `ibkr_order_id` against the latest IBKR sync open-orders + execution snapshots: full-fill → `FILLED` → auto-advance to `RECONCILED` (one pass, persists `filled_quantity` + weighted average fill price); absent from open-orders with no fills → `CANCELLED` → `RECONCILED`; `Cancelled`/`ApiCancelled`/`Inactive` open-order status → `CANCELLED` → `RECONCILED`; `Submitted`/`Presubmitted`/`Working` keeps still-working; partial fills keep still-working. Every transition writes a `critical`-severity event with deterministic `details_json`. State machine guards every transition via `require_transition_allowed`; invalid transitions are reported as failures, never silently accepted. New `prediction_diary_sync.evaluate_prediction_diary(...)` orchestrator builds one diary row per persisted suggestion using the linked forecast for the *issued* snapshot and the persisted EOD bars for the realised prices (with up-to-7-day weekend-walk-back). Suggestions without a linked forecast are skipped with deterministic reasons; persistence errors are captured but never abort the batch. New routes `POST /action-drafts/reconcile` (gated on `reconciliation_sync_enabled` + writable storage + at least one prior IBKR sync), `POST /prediction-diary/evaluate` (gated on `prediction_diary_sync_enabled` + writable storage + IBKR positions), `GET /prediction-diary` (read-only diary listing). Disabled-by-default. `safe_for_self_learning` / `safe_for_model_retraining` / `safe_for_orders` / `safe_for_broker_submission` stay hard-False on every record and every response — the doctrine forbids silent self-learning and any auto-promotion of diary outcomes into the suggestion pipeline.
- Task 162: **completed** — user-approved IBKR paper submission with the locked state-machine handshake (Slice 7 of the V1 critical path). Added the locked V1 state machine in `packages/portfolio/action_draft_state_machine.py`: `ActionDraftState` enum (`draft / safety_checked / user_approved / submitted / awaiting_ibkr_reply / reply_confirmed / working / filled / cancelled / rejected / reconciled / expired / failed`) + `ALLOWED_TRANSITIONS` directed graph (only forward edges plus the SAFETY_CHECKED→DRAFT and USER_APPROVED→DRAFT "edit revokes approval" paths) + `require_transition_allowed()` raising `InvalidStateTransitionError`. Added storage migration `0031_action_draft_submissions_and_events` with two new tables: `asset_action_draft_submissions` (1:1 with each draft via UNIQUE `draft_id`, holds state machine state + approval status/timestamp + IBKR ids + IBKR status text + filled / remaining / average_fill_price + cancellation/rejection/reconciliation timestamps, with `safe_for_broker_submission` / `safe_for_orders` defaulting to False and the dataclass enforcing both stay False), and `asset_action_draft_events` (append-only critical-severity audit log per state transition / approval / submission with rationale_nl + details_json). New `IbapiOrderSubmissionClient` real `ibapi` paper-order placeOrder helper (LMT/DAY/whole-share STK only, captures `orderId`+`permId` via `nextValidId`/`openOrder`/`orderStatus` callbacks, dependency-isolated via importlib + injectable app for tests). New `build_real_order_submission_client(settings)` factory gated five ways: submission_enabled + real_client_enabled + paper account_mode + paper expected_environment + host/port/client-id all set. New `action_draft_submission` orchestrator with `approve_action_draft(...)` (re-validates dry-run + paper-mode + expected-mode, persists `AssetActionDraftSubmissionRecord` with `approval_status=approved` + state `USER_APPROVED`, writes critical event) and `submit_action_draft_to_paper(...)` (re-checks every gate including approval freshness against `action_draft_approval_valid_minutes`, calls the injected real client's `submit()`, transitions to `AWAITING_IBKR_REPLY` on accept or `REJECTED` on IBKR error, writes critical event with `ibkr_order_id` + `ibkr_perm_id` + rejected_reason). New routes `POST /action-drafts/{id}/approve`, `POST /action-drafts/{id}/submit-to-ibkr-paper` (gated six ways including the factory returning a non-None client), `GET /action-drafts/{id}/status` (returns submission + event audit log). Disabled-by-default; paper-only; critical-severity events written on every state transition. Every persisted record keeps `safe_for_broker_submission` / `safe_for_orders` / `safe_for_submission` set to False — gating happens via the factory and the explicit state-machine, never via record flags.
- Task 161: **completed** — action draft skeleton + Orderimpact + dry-run end-to-end (Slice 6 of the V1 critical path). Added pure-Python `action_draft_safety` in `packages/portfolio`: `DraftSourceContext` + `derive_action_draft_sizing(...)` (LMT/DAY/whole-shares; conservative defaults — buy = floor(default_buy_value / market_price), `Langzaam bijkopen` = floor(held × top_up_pct), `Verminderen` = floor(held × reduce_pct), `Verkopen` = all held; non-actionable labels block) + `compute_orderimpact(...)` (Decimal-only: order value, cash before/after, position before/after, portfolio weight after, concentration impact, base currency) + `run_dry_run_safety_checks(...)` (deterministic codes: `missing_ibkr_conid`, `account_mode_mismatch`, `missing_account_mode`, `missing_exchange`, `unsupported_exchange`, `market_data_not_fresh`, `fx_not_fresh`, `buy_value_exceeds_usable_cash`, `cash_comparison_unavailable`, `sell_quantity_exceeds_held`, `invalid_quantity`, `invalid_limit_price`). Added storage migration `0030_asset_action_drafts` with `asset_action_drafts` table (with `safe_for_submission` / `safe_for_orders` / `safe_for_broker_submission` defaulting to False) + matching SQLAlchemy table, `AssetActionDraftRecord` dataclass (rejects non-LMT order types, non-DAY tif, non-BUY/SELL sides, non-positive quantity/price, and any safety boolean set True), and `SqlAlchemyAssetActionDraftRepository`. New `action_draft_sync.generate_action_drafts(...)` orchestrator reads the latest Decision Packages, filters to actionable labels (`Kopen` / `Langzaam bijkopen` / `Verminderen` / `Verkopen`), derives sizing, computes Orderimpact, runs dry-run, and persists one draft per Decision Package; non-actionable labels and sizing-blocked packages are skipped with deterministic reasons. New routes `POST /action-drafts/compute` (gated on `action_drafts_sync_enabled` + writable storage + IBKR sync + positions + Decision Packages), `GET /action-drafts/latest` (latest per current position), `GET /action-drafts/{draft_id}` (single-draft drilldown). Portefeuille page now shows an "Action drafts (LMT / DAY / hele aandelen)" table with the Orderimpact preview, dry-run status badge and failure list per draft. Disabled-by-default; **still no submission to IBKR** — that's the next slice. Every safety boolean (`safe_for_submission`, `safe_for_orders`, `safe_for_broker_submission`, `actions_allowed`, `order_*_allowed`, `blocks_orders`) remains hard-False on every persisted row and every response.
- Task 160: **completed** — Decision Package foundation end-to-end (Slice 5 of the V1 critical path). Added storage migration `0029_asset_decision_packages` with `asset_decision_packages` table (insert-only, content-hashed bundle per (conid, generated_at), with `safe_for_action_drafts` / `safe_for_orders` / `safe_for_broker_submission` defaulting to False) + matching SQLAlchemy table, `AssetDecisionPackageRecord` dataclass (denormalised evidence: position quantity/avg-cost, cash, market last-price/freshness/provider, FX pair/rate/freshness, full forecast outputs, suggestion + locked action label + confidence label, gate outcomes JSON, audit-links JSON, Dutch rationale + explanation), and `SqlAlchemyAssetDecisionPackageRepository` with save + latest-by-conid + list-by-conids. New `decision_package_sync.sync_decision_packages(...)` orchestrator reads the latest IBKR positions/cash + latest suggestions/forecasts + latest market & FX snapshots, computes a SHA-256 `content_hash` over the audit-relevant fields, and persists one immutable record per suggestion; ready suggestions without complete evidence (forecast or market snapshot) are skipped with `incomplete_evidence_chain` rather than persisting half-evidence. New routes `POST /decision-packages/compute` (gated on `decision_packages_sync_enabled` + writable storage + IBKR sync + positions + suggestions), `GET /decision-packages/latest` (latest per current position), and `GET /decision-packages/{ibkr_conid}/latest` (per-row drilldown). Portefeuille page now shows a "Decision Packages" section with one collapsible card per package, containing all denormalised evidence + gate outcomes + audit links + content hash. Disabled-by-default; safety booleans (`safe_for_action_drafts`, `safe_for_orders`, `safe_for_broker_submission`, `actions_allowed`, `order_*_allowed`, `blocks_orders`) remain hard-False everywhere. Decision Packages are the hard prerequisite gate before any action draft can exist; this slice adds the package itself but no action drafts, no orders, no broker submission. No AI authoring; AI never appears in the package chain in this slice.
- Task 159: **completed** — deterministic Dutch label translator + Suggestions foundation end-to-end (Slice 4 of the V1 critical path). Added `baseline_label_translator` in `packages/portfolio`: pure-Python rule engine that maps a `BaselineForecast` + risk profile (`Voorzichtig` / `Gebalanceerd` / `Groei`) + held-or-not context to exactly one of the **locked** Dutch action labels (`Kopen`, `Langzaam bijkopen`, `Houden`, `Bekijken`, `Verminderen`, `Verkopen`, `Vermijden`, `Cash houden`, `Geen actie`, `Geblokkeerd`). Decision tree is conservative: only `strong_up` + `Hoog` confidence on `Groei` produces `Kopen` (cold-start) or `Langzaam bijkopen` (held); any gate failure or blocked forecast forces `Bekijken` or `Geblokkeerd` with explicit blocking reasons. AI **never** decides the label. Added storage migration `0028_asset_suggestions` with `asset_suggestions` table (with `safe_for_action_drafts` / `safe_for_orders` / `safe_for_broker_submission` defaulting to False) + matching SQLAlchemy table, `AssetSuggestionRecord` dataclass, and `SqlAlchemyAssetSuggestionRepository`. New `suggestion_sync.sync_suggestions(...)` orchestrator reads latest forecasts + position list + configured risk profile, applies the translator, and persists one suggestion per (conid, forecast). New routes `POST /suggestions/compute` (gated on `suggestions_sync_enabled` + writable storage + at least one prior IBKR sync + at least one forecast) and `GET /suggestions/latest` (read latest suggestion per current position). Portefeuille positions table now shows an "Actie" `StatusBadge` column joined by symbol, tone-mapped from the locked label, with full Dutch rationale in the tooltip. Disabled-by-default; safety booleans (`safe_for_action_drafts`, `safe_for_orders`, `safe_for_broker_submission`, `actions_allowed`, `order_*_allowed`, `blocks_orders`) remain hard-False everywhere. No action drafts, no orders, no broker execution, no AI runtime, no fake decisions.
- Task 158: **completed** — baseline forecast engine end-to-end (Slice 3 of the V1 critical path, V1.1 stage of the prediction-engine roadmap). Added `baseline_forecast` in `packages/portfolio`: pure-Python lognormal Geometric Brownian Motion (GBM) baseline with `compute_baseline_forecast(bars, current_price, horizon_trading_days)` returning a typed `BaselineForecast` (p10/p50/p90 prices, P(gain)/P(loss), P(loss>5%/10%)/P(gain>5%/10%), annualized volatility, downside-risk score, confidence-from-sample-size, Dutch direction label/explanation). Stdlib only; uses `math.erf` for the normal CDF. Added storage migration `0027_market_data_bars_and_asset_forecasts` (`market_data_bars` for daily OHLCV bars + `asset_forecasts` for forecast records — both with safety booleans defaulting to False) + matching SQLAlchemy tables, `MarketDataBarRecord` / `AssetForecastRecord` dataclasses, and `SqlAlchemyMarketDataBarRepository` / `SqlAlchemyAssetForecastRepository`. Extended `EodhdClient` with `fetch_eod_bars(symbol, from_date, to_date)` (stdlib HTTP, injectable fetcher). Added `forecast_sync.sync_forecasts(...)` orchestrator that maps IBKR positions via the existing exchange-suffix table to EODHD symbols, fetches historical bars (default 400-day lookback), persists them, computes the baseline forecast against the latest market-data snapshot, and persists each forecast with deterministic Dutch labels. New routes `POST /forecasts/compute` (gated on `forecast_sync_enabled` + EODHD config + writable storage + at least one prior IBKR sync) and `GET /forecasts/latest` (read latest persisted forecast per current position). Portefeuille page now joins forecasts by symbol and shows a "Verwachte richting (1m)" `StatusBadge` per position with full tooltip detail. Disabled-by-default; auth/rate-limit short-circuits the batch; safety booleans (`safe_for_analysis/suggestions/action_drafts`, `actions_allowed`, `suggestions_allowed`, `safe_for_orders`) remain hard-False everywhere. No AI; no suggestions, no action drafts, no orders, no broker execution, no scheduler/background fetch.
- Task 157: **completed** — real market-data + FX provider (EODHD), end-to-end (Slice 2 of the V1 critical path). Added `EodhdClient` (stdlib `urllib`-only HTTP client for `/real-time/{symbol}` and `/real-time/{base}{quote}.FOREX`, with injectable `http_fetcher` so tests run offline), `market_data_sync.sync_market_data_and_fx` orchestrator that reads the latest IBKR position + cash snapshots, maps IBKR primary-exchange codes to EODHD suffixes (NYSE/NASDAQ→US, AEB→AS, EBR→BR, SBF→PA, IBIS→XETRA, LSE→LSE, etc.), fetches quotes per de-duplicated conid up to `market_data_sync_max_assets`, derives required FX pairs as `(currency, base)` for every non-base currency in positions or cash, and persists everything through the existing `SqlAlchemyMarketDataSnapshotRepository.save_latest_market_data_snapshot(...)` and `SqlAlchemyIbkrSyncSnapshotRepository.save_fx_rate_snapshot(...)`. New `POST /market-data/sync` route gated on `market_data_sync_enabled` + provider = `eodhd` + `eodhd_enabled` + `eodhd_api_key` + writable storage + at least one prior IBKR sync run; returns a structured summary (`asset_total/success/skipped/failed`, `fx_total/success/failed`, `failures[]`, `base_currency`) with all `actions_allowed`/`suggestions_allowed`/`safe_for_orders` booleans hard-False. Disabled-by-default. Existing `portfolio_valuation_readiness` consumer auto-picks the persisted snapshots so the Portefeuille page renders real `Marktwaarde`, cost basis and unrealized P/L once the sync runs. No suggestions, action drafts, orders, broker execution, schema/migration changes or scheduler/background fetch added.
- Task 156: **completed** — real IBKR paper read-only sync runtime (Slice 1 of the V1 critical path). Added `IbapiReadOnlySyncClient` (real `ibapi`-backed adapter for account summary, positions, open orders, executions with Decimal-only parsing and connection-error → `IbkrTwsReadonlyAdapterError` mapping), safety-gated factory `build_real_sync_adapter` (returns a real client only when `ibkr_sync_real_client_enabled`, `ibkr_sync_enabled`, `ibkr_sync_account_mode=paper`, `ibkr_sync_readonly=True`, and host/port/client-id are all set), and wired the factory into `POST /ibkr/sync/run` with a context-managed `close()`. Existing readiness gate and persistence path unchanged. Disabled-by-default; the route still uses the no-adapter path when the real-client flag is off. Tests cover the real-adapter parsing via the shared production callback set with an injected fake `ibapi` app (no socket opens in CI), the factory gating across every gate, and the route wiring. No orders/suggestions/action drafts/market-data/FX runtime added.
- Task 154-L completed: software name and account-mode scope lock corrected across source-of-truth docs; `AI Trading Agent` locked as active software name, `Portfolio Outlook Manager` marked legacy/historical only, blanket paper-only product-identity wording corrected to account-mode-aware wording with paper mode as safety/testing context, no automatic execution reaffirmed, and next implementation task moved to Task 155.
- Task 153-L completed: documentatie/product-lock recovery voltooid; owner-agreed Version 1 product-, dashboard-, workflow- en daily-rhythm beslissingen geconsolideerd in `docs/product/version-1-product-experience-locks.md` en cross-referenced in kern productdocs. Product-tracking drift na Task 152-R7 gerepareerd en `next-task.md` verplaatst naar Task 154. Geen runtime/API/web/storage/migration/market-data/FX/suggestions/action-draft/order/broker-execution wijzigingen.
- Task 152-R7 completed: repair-only merged-red CI fix na Task 152-R6 voor duplicate keyword pytest failure in `_fake_client_ready_settings(...)`; geen runtimeverbreding en alle bestaande safetygrenzen bleven intact.
- Task 152-R6 completed: repair-only merged-red CI fix after Task 152-R5 for the `api` job `pytest` failures; fake-client execution helpers now enable the full Task 152 prerequisite gate set in tests, including dummy `ibkr_sync_host`, `ibkr_sync_port`, and `ibkr_sync_client_id` values alongside `ibkr_tws_readonly_real_client_enabled=True`. Missing host/port/client-id blockers remain tested for default/real configuration gaps, default-blocked tests still verify disabled-by-default safety-gated behavior, no-secret checks remain strict (no raw config/account payload leaks), and no production runtime behavior was widened (runtime default-disabled, readiness never connects, no sync/market-data/FX/suggestions/actions/orders/broker execution, no auto-connect/reconnect/persistent session manager, no schema/migrations, no `ib_insync`).
- Task 152-R5 completed: repair-only merged-red CI fix after Task 152-R4 for the `api` job `pytest` failures; fake-client execution helpers now enable the full Task 152 gate set including `ibkr_tws_readonly_real_client_enabled=True`, and remaining fake-client execution/error-path tests that still used `_settings(...)` were corrected to use `_fake_client_ready_settings(...)`. Default-blocked tests still prove disabled-by-default and safety-gated behavior. No production runtime widening: runtime remains disabled by default, readiness endpoint never connects, no account/portfolio sync, no positions/cash/open-orders/executions sync, no market-data runtime, no FX runtime, no suggestions/action drafts/orders/broker execution, no auto-connect/reconnect loop, no persistent session manager, no schema/migrations, no `ib_insync`, and no secret/raw broker payload exposure.
- Task 152-R4 completed: repaired merged-red CI failure after Task 152-R3 in the `api` job at `pytest` by updating stale manual status-check fake-client test expectations to enable all required gates (`ibkr_enabled`, `ibkr_status_check_enabled`, adapter/runtime enabled, expected paper mode, paper-only mode) before asserting connect/check/disconnect paths; default-blocked tests still validate disabled-by-default behavior; no-secret tests now reject exact raw configured values instead of safe diagnostic code names like `missing_host`. Real manual status-check capability remains. No runtime behavior widening: runtime stays disabled by default, readiness endpoint still never connects, no account/portfolio sync, no positions/cash/open-orders/executions sync, no market-data runtime, no FX runtime, no suggestions/action drafts/orders/broker execution, no auto-connect/reconnect loop, no persistent session manager, no schema/migrations, no `ib_insync`, and no secret/raw broker payload exposure.
- Task 152-R2 completed: merged-red repair na Task 152-R voor API Ruff B009 failure in `apps/api/src/portfolio_outlook_api/ibkr_ibapi_manual_status_client.py`; constante-attribute `getattr(...)` calls zijn vervangen door Ruff-compliant directe attribuuttoegang via smalle lokale Any-cast op de module (`cast(Any, client_module).EClient` / `cast(Any, wrapper_module).EWrapper`) zodat typed `ibapi` protocol/factory boundary (`_ManualIbapiAppProtocol`) intact blijft. De echte handmatige read-only status-check capability blijft intact. Runtime blijft disabled by default; readiness endpoint maakt nog steeds nooit verbinding; geen account/portfolio sync runtime, geen positions/cash/open-orders/executions sync, geen market-data runtime, geen FX runtime, geen suggesties/action drafts/orders/broker execution, geen auto-connect/reconnect loop/persistente session manager, geen storage schema/migraties en geen `ib_insync` toegevoegd.
- Task 152-R completed: merged-red repair na Task 152 voor API mypy failure in `apps/api/src/portfolio_outlook_api/ibkr_ibapi_manual_status_client.py`; ongetypeerde `ibapi` import/subclassing is nu geïsoleerd achter een typed protocol/factory boundary (`_ManualIbapiAppProtocol`) zodat `mypy src` groen blijft zonder brede ignores. De echte handmatige read-only status-check capability uit Task 152 blijft intact. Runtime blijft disabled by default; readiness endpoint maakt nog steeds nooit verbinding; geen account/portfolio sync runtime, geen positions/cash/open-orders/executions sync, geen market-data runtime, geen FX runtime, geen suggesties/action drafts/orders/broker execution, geen auto-connect/reconnect loop/persistente session manager, geen storage schema/migraties en geen `ib_insync` toegevoegd.
- Task 151 completed: dependency-isolated `ibapi` façademodule toegevoegd voor veilige dependency/import preflight zonder runtime-connectiviteit of wiring; geen TWS/Gateway connectie, geen socket/network default opening, geen endpoint/runtime-factory wiring, geen echte low-level IBKR client, geen `ib_insync`, geen API/web-gedragswijziging en geen storage schema/migraties.
- Task 150-R completed: merged-red repair na Task 150 voor API `pytest` failure in `test_repository_does_not_introduce_ib_insync`; preflightscan vernauwd naar projectmetadata + productie-runtime broncode i.p.v. alle repository-testbestanden, zodat bestaande safety-assertions met `ib_insync` in tests (zoals `packages/storage/tests/test_alembic_skeleton.py`) geen vals-positieve dependencyintroductie meer triggeren. `ibapi` blijft dependency/install-import preflight-only, geen productie-runtime import van `ibapi`, geen `ib_insync` dependency, geen runtime-connectiviteit/sockets by default, geen echte TWS/Gateway clientimplementatie, geen auto-connect/reconnect/persistente session manager, geen account/portfolio sync runtime, geen market-data/FX runtime, geen suggestions/action drafts/orders/broker execution, geen API/web-gedragswijziging en geen storage schema/migraties.
- Task 150 completed: selected `ibapi` dependency toegevoegd in `apps/api/pyproject.toml` voor dependency/install/import CI preflight; import smoke test toegevoegd in `apps/api/tests/test_ibkr_client_dependency_preflight.py` met no-socket guard op import. Geen productie-runtime import van `ibapi`, geen `ib_insync`, geen runtime-connectiviteit, geen echte TWS/Gateway clientimplementatie, geen auto-connect/reconnect/persistente session manager, geen account/portfolio sync runtime, geen market-data/FX runtime, geen suggestions/action drafts/orders/broker execution, geen API/web-gedragswijziging en geen storage schema/migraties.
- Task 149 completed: dependency-selectie compatibiliteitspreflight voor TWS/Gateway client libraries toegevoegd in `docs/product/ibkr-tws-client-compatibility-preflight-task-149.md`, met vergelijking `ibapi` vs `ib_insync`, expliciete go/no-go criteria en aanbeveling voor veilige vervolgstap (Task 150 dependency-only CI install/import preflight). Documentatie/preflight-only: geen runtime code gewijzigd, geen API-gedrag gewijzigd, geen web-gedrag gewijzigd, geen storage schema/migraties, geen `ibapi` of `ib_insync` toegevoegd aan projectmetadata, geen productie-imports, geen socket/netwerkverbinding by default, geen echte TWS/Gateway runtime, geen echte low-level IBKR client, geen auto-connect/reconnect/persistente session manager, geen account/portfolio sync runtime, geen market-data/FX runtime en geen suggesties/action drafts/orders/broker execution toegevoegd. Lokale/ephemeral install/import checks: niet uitgevoerd in deze taak (gedocumenteerd als vervolgstap in Task 150).
- Task 148 completed: real-runtime implementatiebeslissings-gate toegevoegd vóór enige TWS/Gateway client dependency-introductie in `docs/product/ibkr-tws-client-dependency-decision-gate-task-148.md`; opties `ibapi`, `ib_insync`, dependency-free voortzetting en deferral expliciet vergeleken; conservatieve keuze: eerst Task 149 dependency-selectie compatibiliteitspreflight zonder runtime-connectiviteit. Documentatie/planning/decision-gate only: geen runtime code gewijzigd, geen API-gedrag gewijzigd, geen web-gedrag gewijzigd, geen storage schema/migraties, geen `ibapi`, geen `ib_insync`, geen socket/netwerkverbinding by default, geen echte TWS/Gateway runtime, geen echte low-level IBKR client, geen auto-connect/reconnect/persistente session manager, geen account/portfolio sync runtime, geen market-data/FX runtime en geen suggesties/action drafts/orders/broker execution toegevoegd.
- Task 147-R completed: product-tracking drift na Task 147 gerepareerd; `next-task.md` wijst niet langer naar afgeronde Task 147 en is nu copy-paste klaar voor Task 148; `Huidige toestand` staat op `na Task 147-R`. Repair-only documentatie/product-tracking: geen runtime code gewijzigd, geen API-gedrag gewijzigd, geen web-gedrag gewijzigd, geen storage schema/migraties, geen echte TWS/Gateway runtime, geen echte low-level IBKR client, geen `ibapi`/`ib_insync`, geen socket/netwerkverbinding by default, geen auto-connect/reconnect/persistente session manager, geen account/portfolio sync runtime, geen market-data/FX runtime en geen suggesties/action drafts/orders/broker execution toegevoegd.
- Task 146-R completed: repair-only fix na merged-red Task 146; API mypy-fout in `api` job gerepareerd door `_run_manual_tws_readonly_status_check_endpoint(...)` parameter `runtime_settings` expliciet te typen als `Settings`. Geen endpointgedrag gewijzigd. Geen runtime-connectiviteit aangezet. Geen echte low-level IBKR-client toegevoegd. Geen `ibapi`/`ib_insync` dependency toegevoegd. Geen socket/netwerkverbinding standaard geopend. Geen auto-connect/reconnect-loop/persistente session manager toegevoegd. Geen account/portfolio sync runtime, market-data runtime, FX runtime, suggesties, action drafts, orders of broker execution toegevoegd.
- Task 146 completed: handmatige read-only TWS/Gateway status-check endpoint shell toegevoegd via `POST /ibkr/session/manual-readonly-status-check`, met Task 145 dependency-free runtime boundary. Default runtime pad injecteert geen echte client en blijft geblokkeerd (runtime disabled/no runtime client). Fake low-level clients zijn alleen test-only; runtime blijft disabled-by-default. Geen echte low-level IBKR client, geen ibapi/ib_insync, geen socket/network connectie by default, geen auto-connect/reconnect/persistente session manager, geen account/portfolio sync runtime, geen market-data/FX runtime, geen suggesties/action drafts/orders/broker execution en geen schema/migraties toegevoegd.
- Task 145 completed: dependency-free handmatige TWS/Gateway read-only runtime status-check boundary toegevoegd met expliciete runtime opt-in (`ibkr_tws_readonly_runtime_enabled`, default `False`), paper-only/account-mode preflight gates en veilige failure-mapping. Alleen injected fake low-level clients in tests; geen echte IBKR-client, geen ibapi/ib_insync, geen sockets by default, geen auto-connect/reconnect/persistente session manager, geen sync/market-data/FX runtime en geen suggesties/action drafts/orders/broker execution.
- Task 144 completed: expliciete preflight-checklist toegevoegd voor toekomstige echte TWS/Gateway read-only runtime-connection enablement, inclusief harde configuratie/account-mode/lifecycle/no-secret/failure-handling/test gates. Deze taak is documentatie/preflight-only: geen runtime connectivity enabled, geen echte low-level IBKR client, geen auto-connect/reconnect loop/persistente session manager, geen account/portfolio sync runtime, geen market-data/FX runtime, geen suggesties/action drafts/orders/broker execution en geen storage schema/migraties.
## Task 143 update
- Task 143: **completed** — read-only session-status endpointdiagnostiek uitgebreid met expliciete adapterselectie, TWS/Gateway skeleton-readiness, deterministische blocked reason-codes en eenvoudige Nederlandse helpvelden voor runtime-blockers. Standaard blijft de veilige non-network adapter; expliciete TWS/Gateway setting maakt nog steeds geen echte low-level IBKR client aan en runtime-connectiviteit blijft geblokkeerd. Geen auto-connect/reconnect loop, geen persistente session manager, geen account/portfolio sync runtime, geen market-data/FX runtime, geen suggesties/action drafts/orders/broker execution en geen schema/migraties toegevoegd.

## Task 142 update
- Task 142: **completed** — disabled-by-default TWS/Gateway session-status adapter factory/selection pad toegevoegd met expliciete setting `ibkr_tws_readonly_adapter_enabled`, veilige adapterdiagnostiek in statusresponse, en testbare opt-in selectie van de Task 141 skeletonadapter alleen via expliciete instelling (optioneel met geïnjecteerde testclient). Standaardruntime blijft non-network safe adapter zonder auto-connect/reconnect/persistente session manager; geen echte low-level IBKR client, geen account/portfolio sync runtime, geen market-data/FX runtime, geen suggesties/action drafts/orders/broker execution en geen storage schema/migraties toegevoegd.




## Task 141 update
- Task 141: **completed** — disabled-by-default TWS/Gateway read-only session-status adapter skeleton toegevoegd via geïnjecteerde dependency-free client-boundary met veilige statusmapping (`connected_readonly`, `configured_not_connected`, `connected_wrong_account_mode`, `connection_failed`, `authentication_required`, `pacing_limited`, `unknown`) en timeout/error handling zonder secrets of broker payloads te lekken. Geen runtime-connectivity by default, geen auto-connect/reconnect loop, geen persistente session manager, geen account/portfolio sync runtime, geen market-data runtime, geen FX runtime, geen suggesties, geen action drafts, geen orders, geen broker execution en geen storage schema/migraties toegevoegd.



# Current State


## Task 140 update
- Task 140: **completed** — read-only reconciliatie-readiness toegevoegd tussen opgeslagen IBKR-sync snapshots en bestaande portfolio waardering-blockers via `GET /portfolio/valuation/reconciliation-readiness`. Endpoint vat bestaande blockers samen (market data/FX/kostbasis/payloadvalidatie/snapshotbeschikbaarheid), voegt expliciete blocker-categorieën en telvelden toe, en inventariseert geen nieuwe waarden. Geen nieuwe financiële rekenengine toegevoegd buiten bestaande readiness/calculators; geen echte TWS/Gateway runtime, geen persistente session manager, geen storage schema/migraties, geen market-data runtime, geen FX runtime, geen suggesties, geen action drafts, geen orders of broker execution toegevoegd.


## Task 139 update
- Task 139: **completed** — read-only IBKR sync run history en diagnostics verdiept met nieuwe status-only endpoints `GET /ibkr/sync/runs` en `GET /ibkr/sync/runs/{sync_run_id}` op basis van bestaande in-memory syncruns. Responses bevatten expliciete telvelden, payload-validatie samenvatting (conservatief waar nodig), persistence mode/status, en harde safetybooleans die orders/suggesties blokkeren. Geen echte TWS/Gateway runtime, geen persistente session manager, geen storage schema/migraties, geen market-data runtime, geen suggesties/action drafts/orders/broker execution toegevoegd.


## Task 138-R update
- Task 138-R: **completed** — repair-only fix na merged-red Task 138 (PR #332) voor API pytest regressie in `run_sync(...)`: timeout/provider adapterfouten rapporteren nu `payload_validation_status=not_attempted` (met eenvoudige NL uitleg) i.p.v. onterecht `passed`. Adapter/runtimefouten blijven expliciet gescheiden van payloadvalidatiefouten (`payload_validation_failed`). Geen echte TWS/Gateway runtime, geen persistente session manager, geen storage schema/migraties, geen market-data runtime, geen suggesties/action drafts/orders/broker execution toegevoegd.


## Task 138 update
- Task 138: **completed** — IBKR read-only sync adaptercontracten en deterministische test-only fake-adapter fixtures gehard voor cash, positions, open orders en executions. API-contracttests onderscheiden nu expliciet `payload_validation_failed` versus adapter/runtimefouten (`timeout`, `provider_error`) en readiness-blocking (`sync_readiness_blocked`, `sync_readiness_needs_control`) met behoud van veilige persistence-blocking en safetybooleans (`False`/`blocked`). Geen echte TWS/Gateway runtime, geen persistente session manager, geen storage schema/migraties, geen market-data runtime, geen suggesties/action drafts/orders/broker execution toegevoegd.


## Task 137 update
- Task 137: **completed** — documentatie/planning-only selectie van de volgende milestone-sized Milestone B implementatieslices vastgelegd in `docs/product/milestone-b-next-implementation-slices-task-137.md`. Geselecteerde volgende implementatietaak: **Task 138** (IBKR read-only adaptercontracten en fake-adapter fixtures hardenen voor cash/positions/open orders/executions). Geen runtime code, geen API/web gedragswijziging, geen storage schema/migraties, geen echte TWS/Gateway runtime, geen market-data runtime, geen suggesties, geen action drafts, geen orders en geen broker execution toegevoegd.

## Task 136 update
- Task 136: **completed** — duurzame `/ibkr/sync/status` read-model contractuitlijning met Task 135B payload-validatievelden en safetyvelden. `serialize_sync_status_record(...)` levert nu ook `payload_validation_status`-velden met conservatieve defaults (`not_available` / `Niet beschikbaar`) voor historische durable runs zonder opgeslagen validatiedetails; er worden geen historische payloadvalidatie-errors verzonnen. Durable status blijft order/suggestie-safe (`actions_allowed=False`, order/suggestie booleans geblokkeerd, `blocks_orders=True`). Geen storage schemawijziging/migraties toegevoegd. Geen runtimeverbreding: geen echte TWS/Gateway netwerkruntime, geen market-data runtime, geen suggesties, geen action drafts, geen orders en geen broker execution toegevoegd.

## Task 135B-R update
- Task 135B-R: **completed** — repair-only follow-up after merged-red Task 135B. API Ruff `E501 line-too-long` failures were fixed in IBKR sync validation and related API tests by multiline formatting only. Ruff now passes for `apps/api`. Local `mypy src` and API pytest commands in this environment remain blocked by missing dependencies (notably `fastapi`/package stubs), so no runtime or contract semantics were widened during this repair. No runtime, API behavior, storage schema, migrations, market-data runtime, suggestions, action drafts, orders, or broker execution were added.

## Task 134B-R2 update
- Task 134B-R2: **completed** — Task 134B-R2 repaired the remaining API pytest failures after merged Task 134B-R by preventing readiness recomputation in the final `run_sync(...)` response: readiness was first computed with the injected session-status adapter, then incorrectly recomputed by `read_status(settings)` without that adapter; `run_sync(...)` now returns status using the same precomputed readiness object. No real TWS/Gateway network runtime, no real IBKR account/portfolio sync runtime, no market-data runtime, no suggestions, no action drafts, no Decision Packages runtime, no orders, no broker execution, no financial calculations and no fake data were added.

## Task 134B-R update
- Task 134B-R: **completed** — API pytest-repair na merged Task 134B: `_base_settings(**kwargs)` in `apps/api/tests/test_ibkr_sync_endpoints.py` configureert nu zowel sync-instellingen als session-status readiness-instellingen, zodat de fake ready paper session adapter correct door de readiness gate loopt in tests. Root cause was incomplete test settings voor de session-status kant van de readiness gate. Productieruntimegedrag bleef ongewijzigd buiten test/config repair. Geen echte TWS/Gateway netwerkruntime, geen echte IBKR account/portfolio sync runtime, geen market-data runtime, geen suggesties, geen action drafts, geen Decision Packages runtime, geen orders, geen broker execution, geen financiële berekeningen en geen fake data toegevoegd.

## Task 134B update
- Task 134B: **completed** — Task 133B readiness/preflight gate is nu wired in manual `POST /ibkr/sync/run` execution blocking. Readiness `blocked` en `needs_control` blokkeren adapter-calls en persistence; alleen bij expliciete ready paper test-session kan een geïnjecteerde fake adapter in tests draaien. Geen echte TWS/Gateway netwerkruntime, geen echte IBKR account/portfolio sync runtime, geen market-data runtime, geen suggesties, geen action drafts, geen Decision Packages runtime, geen orders, geen broker execution, geen financiële berekeningen en geen fake data toegevoegd.

## Task 133B-R update
- Task 133B-R: **completed** — product-tracking drift hersteld door ontbrekende Task 133B scope-register notitie toe te voegen in `docs/product/version-1-scope-register.md` en de stdlib-only checker `scripts/check_product_tracking.py` te verharden zodat de eerste completed taak uit `current-state.md` verplicht terugkomt in `task-history`, `version-1-backlog` en `version-1-scope-register`. Documentation/process-helper repair-only: geen runtime-, API-, web-, storage-, migratie- of netwerkgedrag gewijzigd; Task 134B is niet geïmplementeerd.

## Task 133B update
- Task 133B: **completed** — minimale IBKR read-only sync readiness/preflight status gate geïmplementeerd via pure statusbuilder en exposed op `GET /ibkr/sync/status` met statussen `Geblokkeerd`, `Controle nodig` en `Klaar voor handmatige read-only sync`. Deze taak wijzigde geen real sync runtime en voegde geen echte TWS/Gateway netwerkruntime, geen account/portfolio sync runtime, geen market-data runtime, geen suggesties, geen action drafts, geen Decision Packages runtime, geen orders/broker execution, geen financiële berekeningen en geen fake data toe.

## Task 132B update
- Task 132B: **completed** — document-first selectie van de volgende veilige Milestone B IBKR read-only sync foundation batch vastgelegd in `docs/product/ibkr-read-only-sync-foundation-batch-selection-task-132b.md`. Geselecteerde volgende implementatietaak: **Task 133B** (IBKR read-only sync readiness/preflight gate op basis van Task 131B account/session status outputs). Deze taak bleef planning-only: geen runtime toegevoegd (geen echte TWS/Gateway netwerkruntime, geen account/portfolio sync runtime, geen market-data runtime, geen suggesties, geen action drafts, geen orders/broker execution, geen financiële berekeningen en geen fake data).

## Task 131B-R update
- Task 131B-R: **completed** — repair na merged Task 131B voor wrong-account-mode regressie in API pytest: `account_mode_status` gaf `unknown` terug i.p.v. `mismatch` bij expliciete adapter-mismatch zonder `account_mode`. Normalisatielogica hersteld zodat expliciete/inferred mismatch behouden blijft en niet wordt overschreven. Geen runtime scope toegevoegd: geen echte IBKR-netwerkadapter, geen auto-connect, geen account/portfolio sync, geen market-data runtime, geen suggesties, geen action drafts, geen Decision Packages runtime, geen orders of broker execution, geen financiële berekeningen en geen fake data toegevoegd.

## Task 131B update
- Task 131B: **completed** — IBKR read-only account/session safety batch geïmplementeerd op de Task 130 session-status boundary met expliciete unknown-status wording, account-mode match/mismatch mapping, en veilige mappings voor `connection_failed`, `authentication_required` en `pacing_limited`, inclusief fake-adapter tests + no-secret/no-fake-data checks. Geen echte IBKR-netwerkadapter, geen auto-connect, geen account/portfolio sync, geen market-data runtime, geen suggesties/action drafts/Decision Packages runtime, geen orders of broker execution, geen financiële berekeningen en geen fake data toegevoegd.

## Task 130Q-R update
- Task 130Q-R: **completed** — product-tracking marker drift gerepareerd door `Huidige toestand` te corrigeren naar `na Task 130Q-R` en stdlib-only checker `scripts/check_product_tracking.py` te verharden met suffix-task volgorde (`130` < `130P` < `130Q` < `130Q-R` < `131B`). Documentation/process-helper repair-only: geen runtime, API, web, storage, migraties, sync, market-data, FX, suggesties, action drafts, Decision Packages, orders, broker execution, financiële berekeningen of fake data toegevoegd.

## Task 130Q update
- Task 130Q: **completed** — owner workshop Version 1 productbeslissingen vergrendeld in `docs/product/version-1-owner-workshop-decision-locks-task-130q.md` en gekoppelde productdocs bijgewerkt (mission-control dashboard, daily operating model, Action Center/Orderimpact/final confirmation flow, Research Desk evidenceflow, cold-start `Nieuwe kansen`). Documentation/planning-only: geen runtime, API, storage, migraties, berekeningen, suggestie-runtime, action-draft runtime, orders, broker execution of fake data toegevoegd.

## Task 130 update
- Task 130: **completed** — disabled-by-default IBKR TWS/Gateway read-only session-status adapter boundary toegevoegd met veilige API-status exposure op `/broker/ibkr/status` en `/ibkr/session/status`, inclusief testbare adapter-injectie zonder echte netwerkcalls. Geen auto-connect, geen account/portfolio sync, geen market-data runtime, geen suggesties, geen action drafts, geen Decision Packages runtime, geen orders/broker execution en geen fake brokerdata toegevoegd. API response bevat geen credentials/secrets.

## Task 129 update
- Task 129: **completed** — volgende Milestone B implementatieslice geselecteerd en gedocumenteerd in `docs/product/milestone-b-ibkr-read-only-runtime-slice-selection-task-129.md` (planning-only). Geselecteerde vervolgstap is Task 130: disabled-by-default IBKR TWS/Gateway read-only session-status adapter boundary + API status exposure. Geen runtime productgedrag gewijzigd: geen API behavior, geen storage/migraties, geen runtime fetch, geen berekeningen, geen suggesties, geen action drafts, geen orders, geen broker execution en geen fake waarden toegevoegd.

## Task 128 update
- Task 128: **completed** — workflow acceleration documentatie toegevoegd (`codex-workflow-acceleration-task-128.md`, `codex-task-template.md`, `task-queue.md`, `codex-red-green-ci-workflow.md`, `version-1-milestone-plan.md`) plus lichte lokale helper scripts (`scripts/check_product_tracking.py`, `scripts/project_status.py`). Task 125W micro-auditrichting is bewust gedeferreerd/vervangen door deze procespivot. Geen runtime productgedrag gewijzigd: geen API-behavior, geen storage/migraties, geen runtime fetch, geen financiële berekeningen, geen suggesties, geen action drafts, geen orders, geen broker execution en geen fake waarden toegevoegd.

## Task 125V update
- Task 125V: **completed** — Task 125U wording-catalogus toegepast op read-only portfolio valuation readiness UI labels/helpteksten (dashboard, portefeuille totals en trace/details). Alleen UI copy/helptekst/fallback-tekst aangepast; geen API-behavior, storage, migraties, runtime fetch, berekeningen, suggesties, action drafts, orders, broker execution of fake waarden toegevoegd. Geen browser-side financiële berekeningen of JavaScript money/P&L parsing toegevoegd.
- Task 125U: **completed** — document-first review/preflight toegevoegd voor read-only portfolio valuation readiness UI-teksten en helpteksten in consistent, eenvoudig Nederlands (`docs/product/portfolio-valuation-readiness-ui-text-review-task-125u.md`), inclusief checklist voor ontbrekende invoer, trace-lege staten en onleesbare nested tracewaarden. Geen runtime code, geen API-behavior changes, geen web runtime-behavior changes, geen storage changes, geen migraties, geen runtime fetch, geen suggesties, geen action drafts, geen orders, geen broker execution en geen fake waarden toegevoegd.
- Task 125T: **completed** — read-only advanced trace/details display toegevoegd voor row-level kostbasis en ongerealiseerde winst/verlies readiness op de Portefeuille-pagina, met uitsluitend bestaande readinessvelden (`missing_cost_basis_inputs`, `missing_pl_inputs`, `cost_basis_input_trace`, `unrealized_pl_input_trace`) en bestaande context/statusvelden uit `GET /portfolio/valuation/readiness`. Geen browser-side financiële berekeningen, geen API-behavior changes, geen storage changes, geen migraties, geen runtime market-data/latest-price fetch, geen runtime FX/provider fetch, geen suggesties, geen Decision Packages runtime, geen action drafts, geen orders, geen broker execution en geen fake kostbasis/P/L/markt/FX/converted-total waarden.
- Task 125S: **completed** — read-only web/API-client display support toegevoegd voor bestaande kostbasis en ongerealiseerde winst/verlies readiness-velden uit `GET /portfolio/valuation/readiness`, met simpele Nederlandse labels en uitsluitend API-aangeleverde strings + availability booleans. Geen browser-side financiële berekeningen, geen API-behavior changes, geen storage changes, geen migraties, geen runtime market-data/latest-price/FX-provider fetch, geen suggesties, geen Decision Packages runtime, geen action drafts, geen orders, geen broker execution en geen fake kostbasis/P/L/markt/FX/converted-total waarden.
- Task 125R-R: **completed** — gerichte API-type-repair na Task 125R: mypy Optional Decimal narrowing in portfolio valuation readiness hersteld zonder functionele scope-uitbreiding. Geen runtime market-data fetching, geen latest-price fetching, geen runtime FX/provider fetch, geen suggesties, geen Decision Packages runtime, geen action drafts, geen orders, geen broker execution en geen fake kostbasis/P/L/markt/FX/converted-total waarden.
- Task 125R: **completed** — pure Decimal-only cost-basis en unrealized P/L calculator uit Task 125Q read-only wired in `GET /portfolio/valuation/readiness` op basis van uitsluitend opgeslagen inputs, met strikte safety gates en zonder runtime fetch. Geen runtime market-data fetching, geen latest-price fetching, geen runtime FX/provider fetch, geen suggesties, geen Decision Packages runtime, geen action drafts, geen orders, geen broker execution en geen fake kostbasis/P/L/markt/FX/converted-total waarden.
- Task 125Q: **completed** — pure Decimal-only cost-basis and unrealized P/L calculator module toegevoegd in `packages/portfolio` met unit tests op caller-provided opgeslagen inputs (kostbasis klaar/missing, market-data ontbreekt, shortpositie blokkade, geen zero-fallback en Decimal exactness). Geen API wiring, geen UI/storage wijzigingen, geen runtime market-data/FX fetch, geen suggesties/action drafts/orders en geen fake kostbasis/P-L waarden.
- Task 125P: **completed** — document-first/read-only preflight toegevoegd voor toekomstige portfolio valuation cost-basis en unrealized P/L display rules op basis van opgeslagen IBKR snapshots en bestaande market/FX readiness gates (`docs/product/portfolio-valuation-cost-basis-pl-preflight-task-125p.md`). Geen berekeningen toegevoegd, geen API-behavior changes, geen web UI-behavior changes, geen runtime FX/provider fetch, geen market-data runtime, geen latest-price fetching, geen suggesties/action drafts/orders en geen fake kostbasis/P-L/FX/converted totals.
- Task 125N: **completed** — read-only web/API-client display support toegevoegd voor valuation conversion totals met hergebruik van de bestaande `GET /portfolio/valuation/readiness` response en het Task 125M display contract. Gebruikt simpele Nederlandse labels en alleen API-aangeleverde Decimal-veilige strings. Geen browser-side financiële berekeningen, geen API-behavior changes, geen runtime FX/provider fetch, geen market-data runtime, geen latest-price fetching, geen suggesties/action drafts/orders en geen fake FX-rates/converted totals toegevoegd.
- Task 125O: **completed** — read-only advanced valuation trace/details display toegevoegd voor valuation conversion totals op de Portefeuille-pagina, met hergebruik van bestaande `valuation_input_trace` en blocker fields uit `GET /portfolio/valuation/readiness`. De weergave blijft display-only, gebruikt simpele Nederlandse labels en bevat geen browser-side financiële berekeningen, geen API-behavior changes, geen runtime FX/provider fetch, geen market-data runtime, geen latest-price fetching, geen suggesties/action drafts/orders en geen fake FX-rates/converted totals.
- Task 125O-R: **completed** — web build repair na Task 125O door JSX namespace type in `ValuationTraceDetails` te vervangen door `ReactElement` type-import. Geen runtimegedrag of UI-logica gewijzigd.
- Task 125M: **completed** — document-first/read-only UI/API display contract preflight toegevoegd voor valuation conversion totals in simpele Nederlandse weergavetaal, gebaseerd op bestaande readinessvelden en zonder runtimewijzigingen. Geen web UI-behavior changes, geen API-behavior changes, geen runtime FX/provider fetch, geen market-data runtime, geen suggesties/action drafts/orders en geen fake FX-rates/converted totals toegevoegd.
- Task 125L: **completed** — pure Decimal-only conversion-total calculator wired read-only into `GET /portfolio/valuation/readiness` using only stored IBKR position/cash, stored market snapshots and stored FX snapshots with strict safety gates and traceability. Geen runtime FX/provider fetch, geen market-data runtime, geen suggesties/action drafts/orders en geen fake FX-rates/converted totals toegevoegd.
- Task 125K: **completed** — pure Decimal-only conversion-total calculator module toegevoegd in `packages/portfolio` met unit tests voor single-currency, multi-currency, missing base/FX, stale/invalid/unknown FX, missing market/cash en no-inverse-pair gedrag. Niet wired in API-routes; geen wijziging aan `GET /portfolio/valuation/readiness`; geen runtime FX/provider fetch, geen market-data runtime, geen suggesties/action drafts/orders en geen fake FX-rates/converted totals toegevoegd.
- Task 125J: **completed** — document-first valuation conversion-total preflight toegevoegd (`docs/product/valuation-conversion-total-preflight-task-125j.md`) met veilige Decimal-only criteria op basis van opgeslagen inputs; definieert contract/statuses/blockers/teststrategie voor latere implementatie. Geen converted-total runtime, geen API-berekening, geen runtime FX/provider fetch, geen market-data runtime, geen suggesties/action drafts/orders en geen fake FX-rates/converted totals toegevoegd.
- Task 125H: **completed** — duurzame FX snapshot storage schema/repository contract foundation toegevoegd (migratie + metadata + repository + tests), read-only en zonder runtime FX fetch/providerintegratie/market-data runtime/valuation conversion runtime/suggesties/action drafts/orders of fake FX-rates/converted totals.
- Task 125G: **completed** — document-first/read-only preflight toegevoegd voor minimale duurzame FX snapshot storage/repository/API-read contracten (`docs/product/fx-snapshot-storage-contract-preflight-task-125g.md`); geen migraties, geen runtime FX fetch, geen market-data runtime, geen suggesties, geen action drafts, geen orders/execution en geen fake FX-rates/converted totals toegevoegd.
- Task 125F: **completed** — read-only FX snapshot-contract inventaris en readiness-koppeling toegevoegd aan `GET /portfolio/valuation/readiness`; API meldt nu expliciet dat FX-opslagcontract ontbreekt (`fx_snapshot_contract_missing`) i.p.v. runtime-fetch te impliceren; geen FX runtime fetch, geen market-data runtime, geen suggesties, geen action drafts, geen orders/execution en geen fake FX-rates/converted totals toegevoegd.
- Task 125E: **completed** — read-only `GET /portfolio/valuation/readiness` contract verrijkt met cash-readiness en FX-readiness metadata op basis van duurzame IBKR snapshots; ontbrekende cash/FX blijft expliciet `Geblokkeerd`/`Controle nodig`; geen runtime FX fetch, geen market-data runtime, geen suggesties, geen action drafts, geen orders/execution en geen verzonnen cash/FX/totals toegevoegd.
- Task 127 leverde de eerste decision-lock documentatie op, maar liet documentatiedrift achter.
- Task 127R herstelt bron-of-truth productdocs, verwijdert paper-only identiteitscontradicties en breidt alle lockdetails uit.
- Na Task 127R zijn er geen runtime code/tests/migrations/workflows/package metadata/UI/trading/execution wijzigingen toegevoegd.

- Task 127: **completed** — documentatie-only productrichting uitgelijnd voor account-mode-aware suggestie/action-draft/Prediction Diary beslissingen; nieuw lockdocument toegevoegd (`docs/product/action-draft-prediction-diary-alerts-decision-locks-task-127.md`) en productdocs geharmoniseerd. Geen runtime code, geen tests, geen migrations, geen workflows, geen UI-code en geen trading/execution runtime toegevoegd.
- Task 126: **completed** — documentatie/research-only asset suggestion + financial algorithm roadmap toegevoegd (`docs/product/asset-suggestion-algorithm-roadmap.md`), inclusief gates/calculation/AI-scheidingsmodel en historische notitie die later door Task 127/127R is vervangen door de huidige account-mode-aware lockrichting. Geen runtime, geen suggesties actief, geen market-data runtime, geen orders en geen live/real-money gedrag toegevoegd.
- Task 125C-C: **completed** — duurzame IBKR sync read/status paden verhard met expliciete storage-unavailable/no-run/latest-run afhandeling en contracttests voor read-only endpoints; safety booleans blijven false; geen TWS/IB Gateway connectie, geen order submit/modify/cancel/bind, geen suggesties en geen fake data toegevoegd.
- Task 125C-B: **completed** — handmatige IBKR read-only sync runtime is nu gekoppeld aan duurzame opslag achter een kleine boundary wanneer storage geconfigureerd, enabled, writable en migration-ready is; in-memory fallback blijft actief bij storage-unavailable paden. Geen echte IBKR-netwerkadapter, geen TWS/IB Gateway connectie, geen order submit/modify/cancel/bind, geen suggesties en geen fake data toegevoegd.
- Task 125C-A: **completed** — kleine veilige slice toegevoegd met API-side `ibkr_sync_persistence` mapperfuncties en een minimale persistence-façade die Task 125B repository-methodes in vaste volgorde aanroept. Endpoint-runtimegedrag is niet vervangen: `/ibkr/sync/*`, `/ibkr/portfolio/positions`, `/ibkr/account/cash`, `/ibkr/orders/open` en `/ibkr/executions` blijven ongewijzigd en gebruiken nog huidige flow/in-memory gedrag. Geen echte IBKR netwerkruntime toegevoegd, geen orders, geen suggesties, geen Decision Packages, geen AI runtime, geen forecasting, geen scheduler/background sync, geen market-data runtime en geen fake data toegevoegd.
- Task 125B: **completed** — PR #258 heeft de IBKR sync storage repository-laag toegevoegd voor vijf duurzame read-only snapshot-tabellen: `ibkr_sync_runs`, `ibkr_account_cash_snapshots`, `ibkr_position_snapshots`, `ibkr_open_order_snapshots` en `ibkr_execution_snapshots`. Toegevoegd zijn repository-dataclasses/records, SQL repository-methodes (`save/get/list/latest`), publieke exports en storage-tests inclusief Decimal round-trip, `None`-preservatie en safety booleans die `false` blijven. Task 125B heeft **geen** API/runtime persistence wiring toegevoegd; dat blijft deferred naar Task 125C.
- Scopebevestiging Task 125B: geen orders, geen suggesties, geen AI runtime, geen forecasting, geen fake brokerdata, geen fake marktdata en geen paid-subscription-afhankelijke IBKR-features toegevoegd.
- Task 125A-R: **completed** — CI-herstel na PR #256 door IBKR sync snapshot migratieconsistentie te repareren (revisie 0025, inventory-contract, metadata-set en tests). Dit bevestigt dat Task 125A alleen duurzame read-only schema/metadata/migration foundation toevoegde; repository-dataclasses/methodes en API runtime wiring bleven toen expliciet deferred naar Task 125B.

- Task 127R2: **completed** — documentation-only eindopschoning van resterende account-mode-woordingscontradicties; geen runtime/code/tests/migrations/workflows/UI gewijzigd.

# Current State (na Task 127R2)

- Task 122: **completed** — IBKR TWS/Gateway technical preflight documentatie toegevoegd en read-only IBKR sessiestatuscontract uitgebreid (disabled-by-default, geen auto-connect, geen orders, safety booleans false).

- Task 120: **completed** — disabled-by-default IBKR paper marktdata-adapter skeleton en handmatige latest-snapshot fetch route toegevoegd (status-first, read-only, geen scheduler/background fetch, geen fake data, safety booleans false).

# Current State (na Task 120)

## 1) Current status summary

- Task 117: **completed** — eerste market-data foundation slice gestart met typed domain provider boundary voor read-only snapshotopvraging op conid-identiteit en expliciete blocked status bij ontbrekende/ongevalideerde identiteit. Scope blijft conservatief: geen live trading, geen orders, geen suggesties en safety booleans blijven false.

- Task 115: **completed** — audit viewer status-quality en contractconsistentie verhard met auditketen/metadata-kwaliteit velden in API, lijstsamenvattingen, regressietests tegen model/dataclass-drift, en web-overview/detail zichtbaarheid. Scope bleef strikt read-only/non-runtime; geen provider calls, runtime-fetch, market-data runtime, suggesties, actiedrafts, orders, IBKR connectie/orders of fake data. CI baseline gecontroleerd vóór implementatie op run #461 (alle zes jobs groen).

- Task 114: **completed** — linked-record API regressiedekking toegevoegd voor request-audit responsemodellen, auditoverzicht verhard met zichtbare read-only recordkaarten en detaillinks, detail-fallbacknavigatie verbeterd, web API types uitgelijnd met Task 113H contract. Scope bleef strikt read-only/non-runtime zonder provider calls, market-data runtime, runtime-fetch, latest-price fetching, scheduler/background jobs, historical fetching, forecast runtime, AI runtime, suggesties, Decision Packages runtime, actiedrafts, IBKR connectie/orders of fake data. CI-check vóór implementatie vastgelegd op run #455 baseline (GitHub API niet bereikbaar in deze omgeving).


- Task 110E: **completed** — resterende Task 110 scope afgerond met read-only/status API exposure voor request logs, provider/source metadata en freshness-audit records inclusief API tests en producttracking-updates. Task 110 is hiermee volledig afgerond: metadata-tabellen, Alembic migratie 0023, migration readiness registration, repository dataclasses/protocols, SQL repository skeleton, public exports, storage tests, read-only/status API endpoints en API tests. Alles blijft non-runtime; geen provider calls, geen market-data runtime, geen runtime-fetch, geen latest-price fetching, geen scheduler/background jobs, geen historical fetching, geen forecast runtime, geen AI runtime, geen suggesties, geen Decision Packages runtime, geen actiedrafts, geen IBKR connectie/orders en geen fake data toegevoegd. Safety booleans blijven conservatief false/blocked.

- Task 109: **completed** — documentatie/design-only contract preflight afgerond (`docs/product/request-log-provider-freshness-contract-preflight-task-109.md`). Candidate contractcatalogi vastgelegd voor request logs, provider/source metadata en freshness-audit records inclusief status/reason-code proposals, traceability-linking en readiness-relatiemapping. Geen storagetabellen, migrations, endpoints, schedulers, runtime-fetching, latest-price fetching, forecast runtime, AI runtime, suggesties, Decision Packages runtime, actiedrafts, orders of fake data toegevoegd. Task 107 tracking-drift preventieregel expliciet gevolgd.

-  Task 108: **completed** — grote conservatieve non-runtime implementatie-prep audit afgerond (`docs/product/non-runtime-foundation-preflight-task-108.md`). Exact één betekenisvolle Task 109 aanbevolen (request-log/provider-metadata/freshness-audit storage/API contract preflight, documentatie/design-only). Task 107 tracking-drift preventieregel expliciet gevolgd (current-state titel + Huidige toestand + completionregel + task-history + scope-register + backlog + next-task in dezelfde PR geüpdatet). Geen runtime market-data fetching, geen runtime-fetch, geen latest-price fetching, geen scheduler/background jobs, geen historical fetching, geen forecast runtime, geen AI runtime, geen suggesties, geen Decision Packages runtime, geen actiedrafts, geen orders en geen fake data toegevoegd.

- Task 81 is documentation-only en vergrendelt dat actieve Volglijst-items IBKR-contract-based moeten zijn.
- Task 81 voegt geen IBKR-runtime toe.
- Task 81 voegt geen market-data runtime toe.
- Task 81 voegt geen historische data-fetching toe.
- Task 81 voegt geen schedulers toe.
- Task 81 voegt geen suggestions, Decision Packages, action drafts, AI runtime of ordergedrag toe.
- Claude architecture/roadmap audit is reviewed; geaccepteerde findings zijn opgenomen in roadmap/todo-documentatie.
- Task 88I is documentatie-only; geen runtimegedrag gewijzigd.
- Task 101: **completed** — documentatie/cross-link/terminologie-anchor-only update. Task 100-termenset is verankerd in handover + locked decisions + lichte cross-links. Geen runtime market-data fetching, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties/Decision Packages/action drafts/orders en geen fake data toegevoegd.
- Task 102: **completed** — conservatieve documentatie-only read-only wording drift check na Task 101. Productdocs zijn gecontroleerd tegen de vergrendelde termenset in `locked-decisions.md`; kleine tracking/plaatsingsdrift is hersteld (current-state titel + backlogplaatsing/next-step wording). Geen runtime market-data fetching, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties/Decision Packages/action drafts/orders en geen fake data toegevoegd.
- Task 103: **completed** — conservatieve documentatie-only product-doc consistentiecheck na Task 102. Productdocs zijn gericht gecontroleerd tegen de vergrendelde read-only terminology in `locked-decisions.md`; kleine tracking/wordingdrift is hersteld (current-state na-Task-102 tracking + follow-up logging). Geen runtime market-data fetching, geen runtime-fetch, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties/Decision Packages runtime/actiedrafts/orders en geen fake data toegevoegd.

- Task 104: **completed** — conservatieve documentatie-only tracking consistency mini-follow-up na Task 103. Producttrackingdocs zijn gecontroleerd tegen de vergrendelde read-only terminology in `locked-decisions.md`; kleine trackingdrift is gericht hersteld (current-state na-Task-103 status). Geen runtime market-data fetching, geen runtime-fetch, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties, geen Decision Packages runtime, geen actiedrafts, geen orders en geen fake data toegevoegd.

- Task 107: **completed** — conservatieve documentatie/review-hardening sustainability-check na Task 106. Post-Task-106 producttracking is gecontroleerd tegen de vergrendelde read-only terminology in `locked-decisions.md`; bekende trackingdrift is hersteld (current-state naar post-Task-106) en compacte tracking-drift preventieregel is toegevoegd in productdocs. Geen runtime market-data fetching, geen runtime-fetch, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties, geen Decision Packages runtime, geen actiedrafts, geen orders en geen fake data toegevoegd.
- Task 106: **completed** — conservatieve documentatie-only read-only terminology lock discipline follow-up na Task 105. Post-Task-105 producttrackingdocs zijn gericht gecontroleerd tegen de vergrendelde read-only terminology in `locked-decisions.md`; kleine tracking/wordingdrift is hersteld (current-state titel + samenvattingsregel naar post-Task-105 status). Geen runtime market-data fetching, geen runtime-fetch, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties, geen Decision Packages runtime, geen actiedrafts, geen orders en geen fake data toegevoegd.
- Task 105: **completed** — conservatieve documentatie-only read-only terminology lock check follow-up na Task 104. Producttrackingdocs zijn gericht gecontroleerd tegen de vergrendelde read-only terminology in `locked-decisions.md`; kleine tracking/wordingdrift is hersteld (current-state titel + samenvatting naar post-Task-104 status). Geen runtime market-data fetching, geen runtime-fetch, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties, geen Decision Packages runtime, geen actiedrafts, geen orders en geen fake data toegevoegd.
- CI-status: **hersteld en groen** na repository visibility change naar public; GitHub Actions execution/logging is weer zichtbaar en bruikbaar.
- Projectstatus: nog foundation-heavy; Version 1 is niet compleet.
- Meest volwassen deel: Onderzoeksbibliotheek / Research Library foundations.
- Suggestion runtime, probabilistische forecast runtime, AI runtime, market-data runtime en IBKR runtime bestaan nog niet.
- Moderne web app shell + dashboard foundation bestaat nu met eerlijke empty states (geen fake data).

- Task 95: **completed** — read-only market-data readiness / AssetListing validation-gate harmonisatie afgerond (API/tests/docs). Readiness list/detail responses bevatten nu expliciet typed `asset_listing_gate` met statusvarianten `storage_unavailable`, `missing_ibkr_conid`, `missing_listing`, `unvalidated_listing`, `validated_listing` en eenvoudige Nederlandse status/next-step/audit-tekst. Missing of unvalidated AssetListing blijft market data/analysis/suggesties/action drafts blokkeren; gevalideerde AssetListing blijft identity/status-only en start geen runtime market-data fetching. Geen market-data runtime/fetching/historical/scheduler/forecast runtime/AI runtime/suggesties/Decision Packages/action drafts/IBKR-ordergedrag/fake data toegevoegd.



- Task 97: **completed** — API model-documentatie/type-metadata + web UI labelconsumptie + tests/docs hardening voor read-only readiness boundaries afgerond. Volglijst gebruikt nu `asset_listing_readiness.status_nl` + `next_step_nl` als hoofdstatus. Geen market-data runtime of runtime-fetch, geen latest-price fetching, geen scheduler/background jobs, geen historical/forecast/AI runtime, geen suggesties/Decision Packages/action drafts/orders en geen fake data toegevoegd.

- Task 96: **completed** — conservatieve read-only terminologieharmonisatie afgerond voor watchlist AssetListing readiness, market-data readiness AssetListing-gate en latest-snapshot responses (API/tests/docs). NL boundary-tekst is nu geharmoniseerd rond read-only status: geen market-data runtime, geen runtime-fetch, geen analysevrijgave, geen suggesties, geen Decision Packages, geen actiedrafts en geen orders. Latest snapshot blijft metadata/status-only (geen live/current prijs). Missing/unvalidated AssetListing blijft blocked; validated AssetListing blijft identity/status-only. Geen market-data runtime/fetching/historical/scheduler/forecast runtime/AI runtime/suggesties/Decision Packages/action drafts/IBKR-ordergedrag/fake data toegevoegd.


- Task 98: **completed** — documentatie/inventaris-only read-only readiness UI/API contractinventaris toegevoegd (`docs/product/read-only-readiness-ui-contract-inventory.md`) voor resterende schermen en readiness-contracten. Labelpatronen voor "veilig", "onveilig tenzij ontkend" en follow-up governance zijn expliciet vastgelegd. Geen runtime market-data fetching, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties/Decision Packages/action drafts/orders en geen fake data toegevoegd.

- Task 99: **completed** — documentatie/review-guardrail-only hardening toegevoegd met compacte PR-checklist + term-review rubric (`docs/product/read-only-readiness-pr-checklist.md`) en koppeling vanuit de read-only readiness inventaris/CI kwaliteitsregels. Geen runtime market-data fetching, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties/Decision Packages/action drafts/orders en geen fake data toegevoegd.
- Task 100: **completed** — beperkte product-doc terminologie-audit uitgevoerd buiten de UI/API-inventaris; nieuw auditnote-document toegevoegd (`docs/product/read-only-readiness-product-doc-terminology-audit.md`) en stale read-only readiness trackingterminologie geharmoniseerd waar nodig. Geen runtime market-data fetching, geen latest-price fetching, geen scheduler/background jobs, geen forecast runtime, geen AI runtime, geen suggesties/Decision Packages/action drafts/orders en geen fake data toegevoegd.

## 2) Implemented foundations

### Platform foundations

- Repository/API/worker/web/docker/CI skeleton bestaat.
- Settings/status foundations bestaan.
- System events foundation bestaat.
- IBKR contracts/placeholders bestaan (geen runtime verbinding).
- Probabilistische asset-outlook doctrine staat vast in docs.
- CI quality rules staan vast in docs.

### Research Library foundations (implemented)

- Research source archive storage/API/UI foundations.
- Safe file upload.
- TXT/MD/CSV extraction.
- Extraction UI trigger + statusweergave.
- Deterministische documentclassificatie.
- Prompt-injection scan status storage/API foundation.
- Source credibility assessment status storage/API foundation.
- Source evidence item storage/API foundation.
- Evidence Ledger linking foundation.
- Gate outcome/freshness foundation.
- Source conflict detection foundation.
- Asset master identity foundation (storage/API) bestaat als referentie/status-only basis.
- Source-to-asset linking foundation (storage/API) bestaat als audit/reference/status-only basis.

## 3) Safety and behavior state now

- Alle source/evidence outputs blijven **blocked for suggestions**.
- Source conflict findings zijn **audit/status-only**.
- Conflict findings blijven **blocked for suggestions**.
- Geen runtime suggestions.
- Geen AI runtime.
- Geen watchlist insertion behavior.
- Geen portfolio positions behavior.
- Geen IBKR runtime action behavior.
- Geen market-data runtime.
- Geen forecast runtime.
- Geen order behavior.

## 4) Current non-complete areas (accurate)

- Prompt-injection runtime scanning engine: pending (alleen status storage/API bestaat).
- Source credibility runtime scoring engine: pending (alleen status storage/API bestaat).
- Evidence ledger runtime/API-linking verdieping: pending.
- PDF/DOCX/XLSX/PPTX extractie: pending.
- OCR: pending.
- URL fetch + veilige snapshotting: pending.
- Source conflict detection runtime engine: pending.
- Source freshness/runtime validation: pending.
- Asset detection from sources: pending.
- Source-to-asset linking runtime/detection/matching beyond foundation: pending.
- Market data/freshness runtime validation: pending.
- Watchlist proposal/user-confirm flow: pending.
- Suggestion engine runtime: pending.
- Probabilistische forecast runtime: pending.
- Portfolio/watchlist volledige runtime grids: pending.
- IBKR read-only runtime integratie: pending.
- IBKR account-mode-aware user-approved broker action flow/submission/reconciliatie: pending.
- Audit viewer runtime: pending.
- AI Event Intelligence runtime: pending.
- Belgische tax/compliance runtime: pending.
- Deployment backup/restore hardening met restore-test bewijs: pending.

## 5) Latest task sequence status


- Task 94: **completed** — AssetListing-to-watchlist readiness wiring afgerond (API/tests/docs): watchlist list/detail responses bevatten nu een typed read-only `asset_listing_readiness` contract dat AssetListing-status per `ibkr_conid` koppelt met deterministische statussen (`missing_listing`, `unvalidated_listing`, `validated_listing`, `storage_unavailable`) en Nederlandse status/next-step/audit-teksten. Ook bij gevalideerde listing blijven `market_data_ready=false`, `analysis_ready=false`, `suggestions_allowed=false` en `action_drafts_allowed=false`; runtime market-data fetch/scheduler, forecast runtime, AI runtime, suggesties, Decision Packages, action drafts en ordergedrag blijven buiten scope. AssetListing blijft strikt gescheiden van AssetMaster; tickertekst alleen blijft onvoldoende.
- Task 92: **completed** — conservatieve market-data readiness explainability en boundary-consistency hardening afgerond: list/detail/latest-snapshot responses bevatten nu stabiele NL help/status-teksten en expliciete non-implication boundaryvelden (`analysis_ready=false`, `suggestions_allowed=false`, `action_drafts_allowed=false`). Blocked/ready/snapshot-metadata semantiek is deterministischer en regressie-getest, inclusief detail endpoint en alle latest-snapshot varianten. Snapshotmetadata blijft read-only status/auditmetadata en impliceert geen analyse, suggesties, Decision Packages, action drafts of orders. Scope bleef read-only API-contract/test/docs hardening; geen market-data runtime, historical fetching, scheduler, AI runtime, suggesties, Decision Packages, action drafts, IBKR-ordergedrag of fake market/broker/recommendation data toegevoegd. Ongevalideerde of onopgeloste identiteiten blijven blocked voor market data/analysis/suggesties/action drafts.
- Task 91: **completed** — conservatieve market-data readiness status-normalisatie afgerond: list/detail readiness-statusvelden zijn nu expliciet getypeerd en genormaliseerd (`blocked`/`ready`, `missing_snapshot`/`snapshot_available`, `missing_or_unvalidated_ibkr_contract`) met regressietests op stabiele Nederlandse help/statusvelden en deterministische blocked/missing-snapshot paden. Latest-snapshot statusvarianten (`not_configured`, `missing_snapshot`, `snapshot_available`, `storage_failure`) blijven expliciet en getest. Scope bleef read-only API-contract/test hardening; geen market-data runtime, geen historical fetching, geen scheduler, geen AI runtime, geen suggesties/Decision Packages/action drafts/IBKR-ordergedrag en geen fake prices/broker/recommendations toegevoegd. Ongevalideerde of onopgeloste identiteiten blijven blocked voor market data/analysis/suggesties/action drafts.
- Task 90: **completed** — conservatieve market-data readiness API cleanup afgerond: snapshot-latest endpoint gebruikt nu expliciet typed responsecontract met vaste statusvarianten (`snapshot_available`, `missing_snapshot`, `not_configured`, `storage_failure`) en regressietests dekken not-configured/storage-failure/missing-snapshot + read-only metadata-only semantiek. Scope bleef read-only API-contract/test hardening; geen market-data runtime, geen historical fetching, geen scheduler, geen AI runtime, geen suggesties/Decision Packages/action drafts/IBKR-ordergedrag en geen fake prices/broker/recommendations toegevoegd. Ongevalideerde of onopgeloste identiteiten blijven blocked voor market data/analysis/suggesties/action drafts.
- Task 88L: **completed (documentation-only)** — CI-herstel vastgelegd na repository visibility change naar public. CI run #358 is groen met 6 geslaagde jobs (`domain`, `storage`, `portfolio`, `api`, `worker`, `web`); logs/steps weer zichtbaar. Geen code/tests/migraties/package metadata/workflows gewijzigd.
- Task 88G: **completed (documentation-only)** — historische diagnose van eerdere GitHub Actions execution/logging blokkade. Deze blokkade is intussen opgelost na visibility change naar public (zie Task 88L).

- Task 88: **completed** — API-only readiness-contract consolidatie voltooid: typed readiness responsemodellen/helpers gecentraliseerd (`market_data_readiness.py`), readiness-routes verdund en regressietests uitgebreid. Read-only scope behouden; geen market-data runtime, geen historical fetching, geen scheduler, geen AI runtime, geen suggesties/Decision Packages/action drafts/IBKR-ordergedrag en geen fake prices/broker/recommendations toegevoegd. Ongevalideerde of onopgeloste identiteiten blijven blocked voor market data/analysis/suggesties/action drafts.
- Task 88B: **completed** — CI/type/import-boundary repair na Task 88: `market_data_readiness.py` koppelt niet langer aan `watchlist.py` modelimport en gebruikt nu een minimale typed protocol-input voor readiness-row build. Dit voorkomt onnodige route/store dependency-koppeling in response-contract code. Geen runtimegedrag toegevoegd; geen market-data runtime, geen historical fetching, geen scheduler, geen AI runtime, geen suggesties/Decision Packages/action drafts of IBKR-ordergedrag toegevoegd. Geen fake market prices, brokerdata of aanbevelingen toegevoegd. Ongevalideerde/onopgeloste identiteiten blijven blocked voor market data, analyse, suggesties en action drafts.

- Task 68: **completed** — Evidence Ledger-linking foundation voor research-source evidence toegevoegd (storage/API), uitsluitend voor audit/lineage; suggesties blijven geblokkeerd.
- Task 69: **completed** — gate outcome/freshness foundation toegevoegd als storage/API basis (audit/status-only).
- Task 69B: **completed** — repair afgerond; CI opnieuw groen zonder runtimewijzigingen.
- Task 70: **completed** — source conflict detection foundation toegevoegd (storage/API), audit/status-only; suggesties blijven geblokkeerd.
- Task 70B: **completed** — API/storage pytest issues gerepareerd; CI groen; geen runtimegedrag gewijzigd.
- Task 71: **completed** — asset master identity foundation toegevoegd (storage/API), identity blijft referentie/status-only en geblokkeerd voor suggesties; geen watchlist/portfolio/suggestie/IBKR/order/AI/market-data/forecast runtime.
- Task 71B: **completed** — API mypy repair afgerond; CI opnieuw groen; geen runtimegedrag gewijzigd.

- Task 72: **completed** — source-to-asset linking foundation toegevoegd (storage/API), audit/reference-only naar canonical asset identities; blijft geblokkeerd voor suggesties en voegt geen watchlist/portfolio/AI/market-data/forecast/IBKR/order runtime toe.

- Task 72B: **completed** — storage mypy row-to-record typing en API pytest 503 failures gerepareerd; CI groen; geen runtimegedrag gewijzigd.
- Task 72C: **completed** — resterende API pytest source-link create→list fixture persistence failure gerepareerd; CI opnieuw groen; geen runtimegedrag gewijzigd.


- Task 74: **completed** — moderne GUI shell en dashboard foundation toegevoegd (apps/web) met Nederlandse navigatie, statusbadges, icon-tooltips, dashboardpanelen en veilige empty states. Geen IBKR runtime, geen market-data runtime, geen suggestion runtime, geen AI runtime, geen ordergedrag en geen fake portfolio/broker/suggestiedata toegevoegd.

- Task 75: **completed** — read-only IBKR portfolio sync foundation toegevoegd met veilige status/endpoints voor sync-run, posities en cash snapshots. Geen ordersubmission, geen action drafts, geen suggestions, geen Decision Packages, geen AI/market-data/forecast runtime en geen fake broker/portfolio data.


- Task 76: **completed** — read-only IBKR sync foundation uitgebreid met open-orders en executions/fills snapshots + endpoints. Geen ordersubmission, orderwijziging, ordercancel, action drafts, suggesties, Decision Packages, AI/market-data/forecast runtime en geen fake broker/order/execution data.
- Task 76B / PR #153: **completed** — API mypy repair op sync run count typing (`int`) met CI terug groen; geen runtimegedrag gewijzigd.
- Task 77: **completed** — read-only Portefeuille-grid toegevoegd op basis van opgeslagen IBKR snapshots (posities, cash, open orders, executions/fills) met Nederlandse empty/error/loading states. Geen echte IBKR connectieruntime, geen ordersubmission/-wijziging/-cancel, geen action drafts, geen suggesties, geen Decision Packages, geen AI/market-data/forecast runtime en geen fake broker/portfolio/order/execution data.

- Task 78: **completed** — watchlist foundation + Volglijst-pagina toegevoegd (lokaal/manueel, gescheiden van IBKR-portefeuille).
- Task 78B: **completed** — CI-repair na Task 78. Root causes: API ruff-formattingfouten in `watchlist.py` en `test_watchlist_endpoints.py`, plus stale storage testverwachtingen voor migratie-inventaris en metadata na `0020_watchlist_foundation.py` / `watchlist_items`. Geen runtimegedrag gewijzigd, geen nieuwe features toegevoegd; watchlist blijft lokaal/manueel en gescheiden van IBKR-posities. Geen suggesties, action drafts, IBKR-ordergedrag, AI runtime, market-data runtime, forecast runtime of fake data toegevoegd.


- Task 79: **completed** — watchlist-to-Asset-Master identity linking foundation toegevoegd (API/UI basis, reference/status-only). Linken/ontkoppelen valideert bestaande asset-identiteit waar veilig, zonder auto-creatie van Asset Master records. Geen portfoliopositiecreatie, geen suggesties, geen Decision Packages, geen action drafts, geen IBKR-ordergedrag, geen AI/market-data/forecast runtime en geen fake data.

- Task 80: **completed** — Asset Master search/picker foundation toegevoegd (read-only zoekendpoint + Volglijst picker UI) zodat gebruikers bestaande canonical asset-identiteiten kunnen zoeken, selecteren, linken en ontkoppelen voor watchlist-items. Reference/status-only: geen auto-creatie van Asset Master records, geen portfoliopositiecreatie, geen suggesties, geen Decision Packages, geen action drafts, geen IBKR-ordergedrag, geen AI/market-data/forecast runtime en geen fake data.

- Task 81: **completed** — documentation-only; IBKR-contract-based active watchlist rule vergrendeld, conid-based data-readiness rule vergrendeld en sync/freshness/performance-roadmap vergrendeld. Geen runtimecode gewijzigd.

- Task 82: **completed** — read-only IBKR contract search/validation foundation toegevoegd met veilige not-configured responses en genormaliseerde conid-gebaseerde contractkandidaten/validatieresultaten via API-endpoints. Geen market-data runtime, geen historische data-fetching, geen schedulers, geen suggestions/Decision Packages/action drafts, geen IBKR ordergedrag, geen AI/forecast runtime en geen fake data.


- Task 83: Volglijst add-flow omgezet naar IBKR contractpicker; actieve creatie vereist gevalideerde IBKR-contractidentiteit. Bestaande losse records zonder contract blijven niet-gevalideerd en niet klaar voor analyse. Geen market-data runtime, historical fetching, schedulers, suggesties, Decision Packages, action drafts, IBKR-ordergedrag, AI runtime, forecast runtime of fake data toegevoegd.

## Current CI status (restored)

- Repository visibility is aangepast van private naar public; de eerdere GitHub Actions execution/logging blokkade is daarmee opgelost.
- CI run **#358** is succesvol afgerond.
- Alle 6 normale CI-jobs zijn groen: `domain`, `storage`, `portfolio`, `api`, `worker`, `web`.
- GitHub Actions logs en step-output zijn opnieuw zichtbaar.
- Normale gate blijft gelden: nieuwe featuretaken starten alleen bij groene CI.

## Task 84 update

- Task 84 toegevoegd: read-only IBKR-watchlist import foundation (watchlists + instrument-preview + import-candidates op conid-basis).
- Geen IBKR watchlist create/update/delete/export, geen market-data runtime, geen suggestions/Decision Packages/action drafts/orders.
- Task 84C: API pytest-repair na PR #163 voltooid; root cause was uitsluitend test setup (foutief `dataclasses.replace()` op Pydantic `Settings` en ontbrekende IBKR-configuratiepatch in configured-path test), geen runtimegedrag gewijzigd en geen scope-uitbreiding.


## Task 85 update

- Task 85 voltooid: conservatieve market-data storage/freshness foundation toegevoegd (schema + status-only API readiness endpoint).
- Geen market-data runtime toegevoegd, geen historical fetching, geen scheduler, geen AI/suggesties/Decision Packages/action drafts/IBKR-ordergedrag.
- Geen fake market prices of fake broker/recommendation data toegevoegd.
- Ongevalideerde of onopgeloste identiteiten blijven geblokkeerd voor market data en latere analyse/suggesties/actie-drafts.

- Task 86: **completed** — market-data readiness persistence wiring uitgebreid met read-only detail toegang en snapshot-metadata endpointfundament. Geen market-data runtime, geen historical fetching, geen scheduler, geen AI runtime, geen suggesties/Decision Packages/action drafts/IBKR-ordergedrag en geen fake prijzen/data toegevoegd. Onopgeloste of ongevalideerde identiteiten blijven geblokkeerd.

- Task 86B: **completed** — API CI-repair na Task 86. Root cause: mypy type issue in market-data readiness detail endpoint waar `dict[str, object]["items"]` als iterabel gebruikt werd. Fix: type/boundary cleanup met interne typed row-builder; geen runtimegedrag toegevoegd. Geen market-data fetch/runtime, geen historical fetching, geen scheduler, geen AI runtime/suggesties/Decision Packages/action drafts, geen IBKR-ordergedrag en geen fake market/broker/recommendation data. Ongevalideerde/onopgeloste identiteiten blijven geblokkeerd voor market data, analyse, suggesties en action drafts.

- Task 87: **completed** — conservatieve watchlist/readiness inspectieverbetering toegevoegd met duidelijke Nederlandse blocker/missing-snapshot uitleg, auditvelden (`evaluated_at`, `missing_identity_fields`, `validation_status`, `next_step_nl`) en read-only snapshotmetadata-status in readiness responses. Geen market-data runtime, geen historical fetching, geen scheduler, geen AI runtime, geen suggesties/Decision Packages/action drafts/IBKR-ordergedrag en geen fake data toegevoegd. Ongevalideerde/onopgeloste identiteiten blijven geblokkeerd voor market data, analyse, suggesties en action drafts.

## Task 88J update (documentation-only)

- Task 88J uitgevoerd als documentatie-only roadmapuitbreiding.
- Geen runtimegedrag gewijzigd.
- Geen applicatiecode/tests/migraties/package metadata/workflows gewijzigd door functionele implementatie.
- CI is hersteld en groen (run #358; alle 6 jobs geslaagd).
- Task 89 mag als volgende implementatietaak weer doorgaan onder de normale groene-CI gate.

- Task 93: **completed** — AssetListing identity foundation verdiept met aparte `asset_listings` storage- en API-basis (identity/reference/status-only). AssetListing is expliciet gescheiden van AssetMaster; IBKR conid hoort op listing/instrumentniveau. Tickertekst alleen blijft onvoldoende voor actieve watchlist, market data, analyse, suggesties of action drafts. Onopgeloste/niet-gevalideerde listings blijven blocked (`safe_to_use_* = false`, `blocks_* = true`). Geen market-data runtime/fetching, geen scheduler, geen forecast runtime, geen AI runtime, geen suggesties/Decision Packages/action drafts/orders en geen fake market/broker/recommendation data toegevoegd.

- Task 123: **completed** — read-only IBKR paper account/cash/positions handmatige sync-foundation toegevoegd via expliciete manual trigger, disabled-by-default configuratie en safety booleans false; geen orders/suggesties/AI/fake brokerdata.
## Task 125B update: durable IBKR sync storage repository layer completed; API/runtime wiring deferred to 125C.

- Task 125D: **completed** — read-only portfolio valuation voorbereiding toegevoegd vanuit duurzame IBKR sync snapshots, met expliciete blocked/control-needed status bij ontbrekende of verouderde marktdata. Geen market-data runtime, geen suggesties, geen action drafts, geen broker orders/execution en geen fake prijzen toegevoegd.

- Task 125I: **completed** — `GET /portfolio/valuation/readiness` consumeert nu alleen opgeslagen FX snapshots via storage repositorycontract (Task 125H), met conservatieve missing/stale/invalid statussemantiek; geen runtime FX/provider fetch, geen market-data runtime, geen unsafe converted totals, geen suggesties/action drafts/orders en geen fake FX-rates/totals toegevoegd.

## Task 130P update
- Task 130P: **completed** — release-candidate-only manual testing policy en milestone batch planning guidance toegevoegd via product/workflow documentatie. Kleine Task 131-route vervangen door Task 131B als volgende veilige milestone-batch. Geen runtime productgedrag gewijzigd: geen API behavior, geen web behavior, geen storage/migraties, geen sync, geen market-data runtime, geen FX runtime, geen suggesties, geen action drafts, geen Decision Packages runtime, geen orders, geen broker execution, geen financiële berekeningen en geen fake data toegevoegd.