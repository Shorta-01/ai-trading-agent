# Trading System Doctrine

**Status:** locked
**Locked on:** 2026-05-26 (decision record: `docs/decisions/0002-trading-system-doctrine.md`)
**Supersedes:** paper-only language in `README.md` and `AGENTS.md` (see §14)

## Purpose of this document

This is the top-level doctrine for the AI Trading Agent. Every functional, architectural, and code-health decision in the project must be consistent with this document. Where this document conflicts with any other doctrine, plan, or product note in the repo, this document wins until explicitly superseded by a later dated doctrine.

## 1. What this system is

The AI Trading Agent is a **full trading system**. It analyses markets, generates order suggestions, and submits approved orders to a broker (Interactive Brokers) for real execution.

It is not a research tool. It is not a paper-only sandbox. It is not a notification service. It is software that places real orders against real accounts when the user approves them.

## 2. The broker relationship: IBKR is the single source of truth

The system connects to one IBKR account at a time. That account is owned and configured by the user inside IBKR. The software does not maintain a parallel or shadow view of the user's positions, cash, or orders.

For everything related to the user's account — positions held, cash balances, open orders, parked orders, order fills, FX exposure, dividends, account events — IBKR's state is the truth. The system reads from IBKR and reflects what it sees. When IBKR's state changes, the system updates. When the system needs to express a user intent against the account (e.g. park an order), it does so by writing into IBKR via the IBKR API; the result is then visible by reading IBKR back.

Consequences:
- The system never invents portfolio data.
- The system never holds an authoritative version of an order that contradicts IBKR.
- The system's audit trail records what it asked IBKR to do and what IBKR reported back, but the *current state* is always *whatever IBKR currently says*.

## 3. Account modes: paper and real are first-class

The IBKR account the system is connected to can be a paper account or a real-money account. The choice is made by the user inside IBKR, by connecting the appropriate IBKR session. The software treats both modes identically:

- Same code paths
- Same calculations
- Same suggestion logic
- Same order flow
- Same audit trail

There are no "paper-mode-only" features. There are no "real-money-only" features. The only difference between the two modes is which IBKR account the system is talking to, and consequently whether orders execute against simulated or real markets.

The purpose of paper mode in this system is solely to verify that the software behaves correctly before pointing it at a real-money account. Paper mode is not an onboarding flow, not a permanent parallel environment, and not a long-term home for any user.

### 3.1 The mode badge

The dashboard must always show a visually unmistakable badge indicating the current mode:
- PAPER mode: clearly marked, distinctive colour (yellow or equivalent)
- REAL MONEY mode: clearly marked, distinctive colour (red or equivalent)

The badge must never be ambiguous, hidden, or styled subtly. The user must never have to think about which mode is active — a glance must answer it.

## 4. The order lifecycle: two grids, two approvals

The system uses a two-step order approval model. Every order passes through two distinct grids on the dashboard, each with its own approval action.

### 4.1 The suggested orders grid

This grid contains order proposals generated by the system. Each row is a fully-formed IBKR-shaped order ticket, pre-filled with the system's best values for every field. The user can:
- Approve the order as-is
- Edit one or more fields, then approve
- Reject the order, with an optional one-word reason (cash / risk / personal / other), logged silently for later pattern analysis

Approving a suggestion does **not** submit the order to the market. It creates the order in IBKR as a parked order, where it becomes visible in the Open orders grid (below).

Rejected suggestions never reach IBKR. They are recorded in the audit trail but do not affect the account.

### 4.2 The Open orders grid

This grid is a live view of IBKR's current order book. It shows every unresolved order — both **parked** orders (awaiting the second approve) and **transmitted** orders that are live at the exchange but not yet fully filled — regardless of origin (system-originated or manually placed by the user inside IBKR / TWS). Each row reflects the current state in IBKR.

Per-row status badge: `parked` / `transmitted` / `partial-filled N/M`. Resolved orders (filled, cancelled, rejected) leave the grid.

The user can on any unresolved row:
- **Approve** a parked order for submission to the market. This is the second approve and is the *real* submission.
- **Edit, then approve.** Edits to a parked or transmitted order are pushed to IBKR via the modify-order API and re-validated with `whatIfOrder` before the modify is sent.
- **Cancel.** Cancels the order in IBKR. Cancel is fire-and-forget; race conditions with a just-arrived fill are inherent and expected (the audit trail records what was attempted and what IBKR reported back).

Approving a parked order from this grid **is** the submission. The order goes live to the market.

### 4.3 The two approvals are different actions

The first approval (suggested grid → Open orders grid as parked) crosses the boundary *system → IBKR*. The second approval (Open orders grid parked → market) crosses the boundary *IBKR parked → IBKR live*. Each approval is logged independently in the audit trail with timestamp, user action, order state, and resulting IBKR response.

### 4.4 Bulk behaviour

Approval is **individual-only** in v1, on both grids. The user reviews and approves one ticket at a time, by intent.

Rejection of suggested orders and cancellation of open orders **may** be bulk. The user may select multiple rows and reject (or cancel) them with one click. A bulk reject carries a single shared one-word reason that applies to every selected row; the audit log records the operation as a coherent batch event with the per-row references inline.

### 4.5 Edit-as-override

When the user edits any field on a ticket before approving, both the original system-proposed value and the user-edited value are recorded per field. Submission proceeds with the edited value. Validation (§5.5) runs against the edited value. The original system value is never silently overwritten in the audit trail.

## 5. Order content: the system pre-fills, the user approves

When the system generates a suggestion, it does not say "buy ASML" in prose. It builds a complete IBKR-shaped order ticket with every field populated.

### 5.1 Quantity

The proposed quantity is computed by combining three layers in this order:

1. **Base size:** fractional Kelly sizing using the existing `kelly_sizing.py` logic, defaulting to 0.25 Kelly (not full Kelly). Derived from per-trade risk budget and a system-derived stop-loss level.
2. **Conviction scaling:** the base size is multiplied by the forecast's confidence score. High-confidence signals get full base; lower-confidence signals get proportionally less.
3. **Hard caps:** maximum percentage per single position, maximum percentage per sector, maximum percentage per asset class. Caps are absolute — they override layers 1 and 2 if breached.

The order ticket shows the resulting share count. The explanation icon on every order ticket must indicate which of the three layers (Kelly base / conviction scaling / hard caps) was the binding constraint for this specific order.

### 5.2 Order type

The system picks the best order type per situation (market, limit, stop, bracket). The choice is determined by the suggestion's logic, not by a global default.

### 5.3 Limit price (for limit orders)

The system pre-fills limit prices using patient-liquidity logic:
- Buys: current mid-price minus a configurable discount (default 0.2%)
- Sells: current mid-price plus a configurable premium (default 0.2%)
- The discount/premium is per-asset configurable, higher for high-volatility assets, lower for low-volatility ETFs
- Forecast override: when the system's forecast-implied fair value disagrees materially with the mid-price (threshold to be defined), the forecast value overrides the patient-liquidity default

The explanation icon must show which logic was used (patient liquidity, forecast override) and why.

### 5.4 Time-in-force

The system pre-fills time-in-force as DAY for every order. Unfilled orders auto-cancel at market close; the next morning's evaluation re-proposes them if still warranted. GTC and GTD are available only via the "show advanced" toggle and are never the system's default.

### 5.5 Validation (hybrid)

Order field validation is hybrid:
- **Client-side, instant:** cheap rules (positive numbers, valid symbol, lot-size multiples, valid TIF values, etc.) checked in the browser as the user edits
- **IBKR round-trip, on Approve:** the full "would IBKR accept this" check runs once when the user presses Approve, via IBKR's `whatIfOrder` (or equivalent). Returns margin impact, commission estimate, and any rejection reason
- If IBKR rejects, submission is blocked and the rejection reason is shown to the user

Validation applies in both the suggested orders grid (first approve) and the Open orders grid (second approve, or any edit to a parked or transmitted order). Modify on transmitted orders re-runs `whatIfOrder` before the modify is sent to IBKR.

## 6. Synchronisation: hybrid push/pull, IBKR-driven

The system stays in sync with IBKR using a hybrid model:

- **Primary path:** event-driven subscription to IBKR's order-update and account-update streams. Any change in IBKR — new order, modification, fill, cancellation, position change, cash change — pushes immediately to the system and on to the dashboard
- **Fallback path:** periodic polling whenever the event stream is disconnected, throttled, or otherwise unhealthy. Polling interval to be defined per stream type

The dashboard's system-health line must reflect which mode is active so the user can see at a glance whether updates are real-time or polled.

This sync model applies to all IBKR-sourced state: orders, positions, cash, fills, account events.

Reconciliation runs as a periodic backup beneath the event-stream sync. Cadence: every 15 minutes during market hours, every hour outside, plus event triggers (07:00 before morning chain, after every order fill, after IBKR session reconnect, on user demand). The 07:00 reconciliation is mandatory and **blocks the morning chain until complete** — if reconciliation cannot finish, the morning chain does not run, the dashboard shows the previous day's state, the system-health line turns red. Reconciliation discrepancies are classified into four tiers (B/C/D/E); see `docs/intent/reconciliation.md` for the full classification model.

## 7. The morning evaluation: every day from scratch

Every day at 07:00 the system performs a full evaluation: refresh prices, refresh FX, refresh IBKR positions, recompute valuations, generate forecasts, build decision packages, populate the suggested orders grid for the day.

Yesterday's unactioned suggestions do **not** carry over. The system re-evaluates from the current market state. If a previously-suggested action is still warranted today, it appears as a fresh suggestion. If conditions have changed, it does not.

This rule is in deliberate tension with persistent (GTC) orders, which is why the system's default TIF is DAY (§5.4). The morning evaluation also reconciles against IBKR's actual order book: any orders still alive from previous sessions (rare, e.g. manually-placed GTC orders) appear in the Open orders grid as the live source of truth.

## 8. Throughout the day: silent, hourly, additive

Between morning evaluations, the system runs lighter evaluations every hour. New suggestions append silently to the suggested orders grid as the system identifies them. There are no notifications, no interruptions, no badges that demand attention. The user checks the dashboard when they choose to.

The Open orders grid updates in real time via the sync model (§6), not on an hourly cadence.

## 9. Suggestion recurrence: counter, no snooze

When the system proposes the same suggestion (same asset, same direction, similar size) on consecutive days, the suggestion shows a recurrence counter inline ("3rd time this week"). This is informational. There is no snooze, no "reject for a week" option. Every day the system evaluates fresh; if the suggestion is still warranted, it appears again.

Rejection is per-day and includes an optional one-word reason (cash / risk / personal / other), captured silently for later pattern analysis.

## 10. The dashboard: three areas, no clutter

The dashboard contains three areas and only three areas:

1. **Portfolio:** current positions sourced from IBKR. Each row shows the position-level current value computed from the **last close** price (live prices are used only at the moment of order ticket construction, via the on-demand IBKR live quote refresh — see §6 sync model). No daily P&L on the dashboard, no daily change colouring, no totals delta. Performance review lives on a separate screen (§11).
2. **Watchlist:** assets being tracked but not held. Each item shows an inline "proximity to action" signal indicating how close it is to triggering a buy suggestion.
3. **Actions:** the suggested orders grid (§4.1) and the Open orders grid (§4.2) plus a smaller stream of **system-decision actions** (predictor retirement decisions, settings warnings, reconciliation D-class items, shadow-predictor promotion decisions, audit exceptions). Order actions are the dominant category; system-decision actions share the same area, visually distinguished by category but without a separate fourth area on the dashboard. Each order row shows a **trust signal** combining three concerns — forecast confidence + data freshness + decision-package trust tier (full / degraded / minimal) — as a single inline cue, not separate badges. Each order row also shows an explanation icon (clear, plain-language rationale for why the system proposes this order).

A single **system-health line** at the top of the dashboard reflects the worst current state across multiple subsystems (sync mode, calibration drift, reconciliation status, last-full-rebuild timestamp, AI budget consumption). Colour reflects the worst state: green when all subsystems are healthy (visually quiet), yellow when any subsystem is in a degraded but non-blocking state, red when any subsystem is in a blocking state (visually loud). Clicking the line opens a breakdown of which subsystem is at what status. Specific yellow/red thresholds are defined in each subsystem's intent doc.

When the actions area is empty, a one-line explanation states why ("no signals triggered today, watchlist stable, portfolio within target ranges"). Empty without explanation is forbidden.

No charts, no news feed, no sentiment ticker, no market overview, no notifications panel, no past-actions history on the dashboard. Those belong on dedicated screens.

## 11. Performance review: separate screen, professional metrics

Performance review lives on a separate screen, not on the morning dashboard. The user visits it when they choose to, not as part of the morning routine.

The performance screen shows:
- Time-weighted return vs benchmark
- Drawdown from peak
- Volatility / risk-budget usage
- Exposure breakdown (asset class, sector, currency)
- Portfolio chart
- Weekly and monthly views

Daily P&L is deliberately not surfaced as a headline metric, here or on the dashboard. The intent is to support deliberate weekly/monthly review, not to encourage daily emotional reactions.

## 12. Audit trail: complete, immutable, separate

Every state-changing event is recorded in the audit trail:
- Suggestion generated
- Suggestion approved / edited / rejected (with reason)
- Order parked in IBKR (system-originated or manual)
- Parked order edited
- Parked order approved for submission / cancelled
- Order filled (partial or full)
- Sync events
- Mode changes (paper ↔ real connection changes)

The audit trail is append-only and lives on a separate screen, not on the dashboard. Audit entries reference the IBKR-side identifiers wherever applicable, so any event can be traced back to its IBKR record.

The pre-existing rule "no advice without audit trail" (`AGENTS.md`) remains in force.

## 13. AI: explanation primary, features permitted with guardrails, never decision

AI (LLM) usage is bounded by the rule **Python calculates, AI explains** — but AI may produce features that deterministic forecasters consume, provided the features are snapshotted for reproducibility and the LLM is not the sole or dominant input to the forecast.

The AI is used to:
- Explain to the user, in plain language, why the system proposed a given order (the explanation icon, §10)
- Summarise research evidence and forecast reasoning
- Translate technical analysis into the user's language (Dutch, Flemish register where appropriate)

The AI is not used to:
- Decide quantities, prices, order types, or any other order field
- Override risk rules or investment policy caps
- Approve or reject orders
- Modify IBKR state directly

### 13.1 Explanation provider

The AI explanation provider is user-configurable between Anthropic Claude (recommended default) and OpenAI GPT. The system automatically falls back to the secondary provider when the primary fails (outage, budget exceeded, schema-invalid output, voice-validation failure). The scope of multi-provider configuration is strictly limited to explanation generation; any non-explanation AI usage requires a separate doctrine update.

### 13.2 AI-derived features in forecasting

AI-derived features in forecasting pipelines (case C of the three-case framework — see `docs/intent/ai-usage.md`) are permitted under three guardrails:
- Cached / snapshotted output (reproducibility)
- Treated as a feature, not as a forecast (the deterministic forecaster owns the prediction)
- Never the sole or dominant input

### 13.3 Out of scope in v1

Direct LLM forecasting (case B — the LLM emits the prediction) is **not permitted in v1**. Reclassifying classical ML models as "AI" (case A) is also not permitted; "AI" in this doctrine means LLM specifically.

### 13.4 Hard constraints

Every AI output is schema-validated. AI may not override deterministic risk or sizing logic. The AI explanation budget cap (the existing `claude_ai_budget` infrastructure, extended to multi-provider in v1) remains in force.

## 14. What changes from prior project guardrails

This doctrine supersedes the following prior rules:

From `README.md`:
- "Versie 1 werkt uitsluitend met papergeld" → superseded; paper mode is for testing only, real money is the system's purpose
- "Geen live trading" → superseded; live trading is the system's purpose
- "Geen real-money orders" → superseded
- "Geen broker execution" → superseded
- "Geen automatische orderplaatsing" → retained in spirit (the user always approves twice; the system never submits without user approval)

From `AGENTS.md`:
- "Do not implement live trading" → superseded
- "Do not add broker execution in version 1" → superseded
- "Do not add IBKR live order flow" → superseded

Retained from `AGENTS.md` (unchanged):
- "No business logic in UI"
- "No hardcoded secrets"
- "No hardcoded tickers in core logic"
- "No Raspberry Pi-specific application logic"
- "No external API calls without documented adapter and test strategy"
- "No silent data correction"
- "Every decision must be logged"
- "No advice without audit trail"
- "All data must be backed up and restorable"
- "A backup is not trusted until restore is tested"
- "Jobs must be idempotent"
- "Scheduled job failures must be visible"
- "Every financial calculation must have tests"
- "Every AI output must be schema-validated"
- "AI may not override risk rules"
- "Keep UI Dutch and simple"
- "Every UI field must have simple Dutch help text"

## 15. What this doctrine does not yet decide

The following are out of scope for this doctrine and will be decided in subsequent intent documents and discussions. The list grew during the T-011…T-024 functional review of 2026-05-26.

Originally listed:
- Stop-loss derivation method (needed by §5.1 Kelly sizing)
- Forecast confidence score definition (needed by §5.1 conviction scaling)
- Mid-price disagreement threshold for forecast override (§5.3)
- Polling intervals when the event stream is unhealthy (§6)
- Watchlist "proximity to action" signal definition (§10)
- The specific metrics, time windows, and benchmark selection on the performance review screen (§11)
- Bracket order policy (when and how stop-loss / take-profit pairs are auto-attached)
- Multi-currency reporting and FX hedging policy

Added 2026-05-26 from the T-011…T-024 functional review:
- Settings storage backend choice (OS keyring vs encrypted file)
- Stop-loss policy default (ATR-based / fixed % / none)
- Drawdown circuit-breaker threshold default
- Critical alert delivery channels beyond email
- Multi-provider AI scope expansion (pending T-015 and T-023 findings)
- Default config of data feature toggles for new installs
- Calibration drift thresholds (per-predictor and ensemble-wide)
- Calibration window length default (currently 12 months)
- Predictor retirement threshold default (currently 6 months)
- Initial banned-patterns list for voice rules (Dutch-specific patterns to discover from real output)
- AI budget defaults per provider
- Belgian tax rules versioning and annual update mechanism
- Speculative-classification threshold defaults (require accountant review before lock)
- Fund classification storage (Reynders-applicable) — system-stored vs accountant judgment per disposal
- Corporate actions handling (splits, mergers, special dividends) — never explicitly designed
- Per-currency hard cap in Kelly sizing (future evolution candidate)
- Accrued dividend handling in valuation
- Permitted vs prohibited patterns for AI in forecasting (full formalization)
- Initial population of rejection classification mapping (IBKR/exchange codes per recoverable/unrecoverable)
- Regime detection capability (would enable regime-triggered backtests and regime-stratified leaderboard)
- Historical-universe data source for survivorship-bias correction
- Shadow-mode promotion threshold (X% of backtest hit rate) value tuning
- When to add a second forecast horizon stream
- Default reconciliation thresholds per pass (current values are intent; reality may differ)
- Exact freshness windows per input type (for decision package trust tier)
- Price-move threshold for delta-rebuild trigger

These will be added as further intent documents under `docs/intent/` and referenced from this doctrine.
