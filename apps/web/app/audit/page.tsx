"use client";
import { useEffect, useState } from "react";
import { apiClient, type FreshnessAuditListResponse, type ProviderSourceListResponse, type RequestLogListResponse } from "@/lib/apiClient";
const fmt = (v: string | null) => (v ? new Date(v).toLocaleString("nl-NL") : "-");
export default function AuditPage() { const [logs, setLogs] = useState<RequestLogListResponse | null>(null); const [sources, setSources] = useState<ProviderSourceListResponse | null>(null); const [freshness, setFreshness] = useState<FreshnessAuditListResponse | null>(null); const [error, setError] = useState<string | null>(null);
useEffect(() => { void (async () => { const [l, s, f] = await Promise.all([apiClient.getRequestAuditRequestLogs(), apiClient.getRequestAuditProviderSources(), apiClient.getRequestAuditFreshnessAudits()]); if (!l.ok || !s.ok || !f.ok) { setError("API niet bereikbaar. Dit scherm blijft read-only."); return; } setLogs(l.data); setSources(s.data); setFreshness(f.data); })(); }, []);
return <main className="page-wrap"><h2>Audit</h2><p className="audit-help">Read-only auditweergave. Geen runtime. Geen analysevrijgave. Suggesties geblokkeerd. Geen actiedrafts. Geen orders.</p>{error ? <div className="empty-state"><p>{error}</p></div> : null}{!logs || !sources || !freshness ? <div className="empty-state"><p>Laden van auditgegevens...</p></div> : <div className="audit-grid"><section className="dashboard-card"><h3>Request logs</h3>{logs.items.length===0?<p>Geen records.</p>:<table className="portfolio-table"><thead><tr><th>ID</th><th>Status</th><th>Provider</th><th>Aangemaakt</th></tr></thead><tbody>{logs.items.map((i)=><tr key={i.request_log_id}><td>{i.request_log_id}</td><td>{i.request_status}</td><td>{i.provider_code}</td><td>{fmt(i.created_at)}</td></tr>)}</tbody></table>}</section><section className="dashboard-card"><h3>Provider/source metadata</h3>{sources.items.length===0?<p>Geen records.</p>:<table className="portfolio-table"><thead><tr><th>ID</th><th>Provider</th><th>Bron</th><th>Domein</th></tr></thead><tbody>{sources.items.map((i)=><tr key={i.provider_source_id}><td>{i.provider_source_id}</td><td>{i.provider_code}</td><td>{i.source_type}</td><td>{i.data_domain}</td></tr>)}</tbody></table>}</section><section className="dashboard-card"><h3>Freshness-audits</h3>{freshness.items.length===0?<p>Geen records.</p>:<table className="portfolio-table"><thead><tr><th>ID</th><th>Status</th><th>Domein</th><th>Geëvalueerd</th></tr></thead><tbody>{freshness.items.map((i)=><tr key={i.freshness_audit_id}><td>{i.freshness_audit_id}</td><td>{i.freshness_status}</td><td>{i.data_domain}</td><td>{fmt(i.evaluated_at)}</td></tr>)}</tbody></table>}</section></div>}</main>; }
